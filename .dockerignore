# 不进镜像的东西（M69）。
#
# 三条判据，任何一条成立就排除：
#
#   1. 运行时用不到（测试、文档、本地工具）
#   2. 会让镜像带上不该带的东西（口令快照、会话记录、本机日志）
#   3. 会让构建缓存失效（每次都变的文件）
#
# 第 2 条最要紧：`.workbuddy-ai/` 里有本机的会话与记忆，
# `.agentos_http_smoke.log` 里有一次真实请求的痕迹。
# 把它们打进镜像，等于把开发机的一部分复制到每一个生产容器里 ——
# 而这件事没有任何人会再看一眼。

# 本机工具/会话数据（第 2 条）
.workbuddy-ai/
.git/
.github/
.idea/
.vscode/

# 测试（第 1 条）：镜像里不跑测试，测试在 CI 里跑
tests/

# 文档与产物（第 1 条）
*.md
agentos-architecture.html

# Python 噪声（第 3 条）
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# 本机日志（第 2 条）
*.log
.env
