# 2026-09-22

## Temporal 调研（对项目定位有战略影响）

**核心机制**：Workflow（确定性编排代码）+ Activity（副作用，自动重试）+ Event History（唯一事实源）+ Replay（Worker 恢复时重放历史重建状态）。状态不落业务库，靠重放事件历史恢复到断点。

**⚠️ 重大变化：Temporal 已于 2026 年 5 月 Replay 2026 正式转向 AI Agent**。定位从"持久化工作流引擎"改为"构建可靠 AI Agent 的生产级运行时"。已发布：
- OpenAI Agents SDK 集成（GA，含 sandbox）
- Google ADK 集成（LLM 调用与工具执行以 Activity 运行）
- Workflow Streams（基于 Signal/Update 的持久化流式，支持 token 批次与护栏）
- External Payload Storage（大 payload 落 S3，Public Preview）
- Standalone Activities（Activity 可独立运行，Public Preview）
- Serverless Workers（AWS Lambda，Pre-release）
- Task Queue Priority & Fairness（GA）、Nexus、多区域复制 RTO 20 分钟
- 获 AWS AI Competency（Agentic AI 类别）

**与本项目 Kernel 的逐项对照**（几乎全覆盖）：
| AgentOS 组件 | Temporal 对应 |
|---|---|
| ExecutionStateMachine | Workflow 代码本身即状态机 |
| Scheduler + Priority + Aging + 租户公平 | Task Queue Priority & Fairness（GA） |
| Lease + heartbeat | Activity heartbeat + timeout |
| Attempt + RetryPolicy | Activity Attempt + RetryPolicy |
| Checkpoint | Event History 自动，无需手写 |
| recovery_controller | Replay 自动恢复，无此组件 |
| wakeup_controller | Signal + Timer（可等数月） |
| cancellation_sweeper | Cancellation 一等公民 |
| outbox_publisher | 不需要，Event History 本身事务性 |
| Idempotency Key | Activity ID 幂等 |
| Saga / Compensation | 内置 Saga |
| Artifact → S3 | External Payload Storage |
| child_run_consumer | Child Workflow（一等公民） |

**Temporal 明确不做**（本项目的真实空间）：Agent 语义（Goal/State/Decision/Action/Observation）、Model Gateway 与模型路由、成本治理、Context 工程与 Memory 分层、RAG、护栏引擎、评估与失败归因、Step→Task→Execution→Attempt 这套 Agent 特有执行对象模型。

**建议（已向用户提出）**：把 Execution Kernel 的持久化层做成可插拔后端，增加 Temporal 后端实现与现有 PG 后端并存。这样"为什么不用 Temporal"的答案变成"我用了，但在它之上补了 Agent 语义层"，从防御转为进攻。

## 项目命名：Asuka（用户提出，2026-09-22）

用户想把项目命名为 Asuka。已给出三层阐释方案：

1. **技术反解（主推）**：A-S-U-K-A = **A**gentic **S**ystem **U**nified **K**ernel **A**rchitecture（面向 Agentic 系统的统一内核架构）。U=Unified 与 K=Kernel 正好对应项目核心命题"统一的 Execution Kernel"。
2. **汉字意象**：取「飛鳥」（而非「明日香」）。飞鸟时代是日本从部族林立走向律令国家的转型期，隐喻"把散乱的 Agent 能力统合成有秩序的执行体系"。
3. **读音**：a-su-ka 三音节，CLI 顺口（`asuka run`），国际化无发音障碍。

## ⚠️⚠️ 第二轮「代码跑在文档前面」：不止两个，是九个（M67~M75）

上一轮查出 M67/M68 未冻结后，做了**全仓编号对齐**（不只是 M 编号，而是
"代码里有、基线里没有"的普查），结果发现远不止两个：

| M | 内容 | 测试 |
|---|---|---|
| M69 | 运行时镜像（一个镜像八个入口） | **零** |
| M70 | 集群部署清单（八份 K8s + 两份 TOML，两个 `replicas: 0`） | **零** |
| M71 | `apps/_dsn.py`：DSN 只有一处（被一次真实部署逼出来） | test_dsn.py ✓ |
| M73 | HTTP 推进之后必须落快照 | test_http_advance_snapshot.py ✓ |
| M74 | 终态 Run 查得到（只是推不动） | 被 M73 的用例间接守住 ✓ |

**M72 不存在**（跳号）。本轮一次性回写冻结 → **v2.1.63**（§107~§112）。

**M71 的形状值得单记**（它是最好的一个例子）：迁移器自己去找
`AGENTOS_PG_DSN`，而 Pod 上挂着 `AGENTOS_MANIFEST` → "这一次部署的配置"
有了两个来源。后果**不是崩溃，是成功**：清单改了 DSN、迁移器读环境变量 →
把迁移应用到**另一个库**，然后报 `up to date`。一次看不出任何问题的成功。

## M75 · 部署清单也要有人检查（v2.1.63）

M69/M70 那套资产零测试 → 新增 `tests/unit/test_deploy_manifest.py`（14 条）：
入口存在 / provider 可装载 / **ConfigMap 内嵌 TOML 与 `deploy/config/*.toml` 语义相等**
/ 运行中的 agentos 进程都挂 liveness / 心跳只声明一处 / COPY 源路径存在 /
**镜像 tag == 服务对外报的版本** / 控制组（证明扫得到东西）。

本机没有 pyyaml → 与 M68 那条一样**按行扫文本**，所以**必须配控制组**。

**⭐ 补的时候当场抓到一处漂移**：`deploy/k8s/` 下 **14 处** tag 写着
`agentos:2.1.55-b3`，而服务已报 2.1.63 —— "线上跑的是哪个构建"有两个答案，
而且**每次冻结基线都会再漂一次**。已修 tag + 留下测试盯着。

**变红验证 7/7。但第一轮只红了 4/6**，两条没红的**漏都在测试自己身上**：

| 漏洞 | 为什么不红 |
|---|---|
| 入口正则 `apps\.([a-z_]+)` | 不含数字 → `apps.migrate_v2` 匹配到 `migrate_v` 后要求紧跟引号，撞上 `2` 整条失配 → **改名扫不出来** |
| 心跳检测只认 `AGENTOS_HEARTBEAT_FILE=` | 认不出 K8s 的 `- {name: …}` 形式 → **在 K8s 里再声明一遍扫不出来** |

## ⭐ 元教训：变异没红，先怀疑测试，不要先怀疑被测代码

本轮 6 条变异里 2 条没红，两条的漏都在**测试自己**。这类漏在测试绿着的时候
没有任何理由被人发现 —— 没人会去怀疑 `[a-z_]+` 里少了个 `0-9`。

配套三条写变红脚本的硬规矩（都是本轮栽出来的）：
1. **每条变异之后立刻还原**，只在最后还原一次 → 第二条起的计数全被前一条残留污染
2. **变异本身要真的把名字去掉**：注释掉 `livenessProbe:` 不行 ——
   断言用的是 `in doc`，`# livenessProbe:` 仍含那个子串，照样绿
3. 子进程输出 `encoding="utf-8", errors="replace"`（Windows 是 GBK）；
   计数正则 `FAILED \((failures=(\d+))?(?:, )?(errors=(\d+))?\)`（两段可缺省）

## M76 · REPLAN 是一条真的边（v2.1.64）

**洞的形状**：`ActionType.REPLAN` 机制**齐全**（action.py 定义、reducer 清空
`current_plan`、loop 分支、`task_factory` 映射为 None），但
**没有任何 DecisionEngine 产出它，也没有任何测试走过它**。
全仓唯一提到 REPLAN 的用例只是拿它当"普通活不是派生"的控制组枚举值。

**⭐ 为什么值得单独一轮**：`derive.py` 里写着
「Agent 下一步可能还要 REPLAN（Step 全绿但目标没达成）」——
这句话是 **B-7 的一条论据**。即：**一条不变量的论据，依赖一个从未接线的机制**。
它不是"少了个功能"，是"文档声称的机制没接线"，而且不报错。

**⭐ 补的时候抓到真 bug（这是本轮最值钱的）**：**重规划不吃预算**。
`self.steps += 1` 只在执行完成那条路径上（loop.py 原 1474 行），REPLAN 分支不经过 →
`steps >= budget` 永远不成立 → 一直 REPLAN 的引擎让 Run **永远跑下去**，
既不终止也不报错（L-7 同族）。测试不是断言失败，是**跑不完**。
新增 **I-10：重规划必须吃预算**（刻意不新增 `max_replans` 旋钮，B-7：预算只有一处）。

**测试** `tests/unit/test_replan.py` 7 条：REPLANNED / current_plan 清空 /
经 Observation（I-3）/ **下一轮真的重新规划**（planner.calls 1→2）/ 一直 REPLAN 仍终止 /
不超额 / 控制组（同预算下正常 Run 照样 FINISHED）。变红 3/3。

**写测试时撞到的两处"我以为是、其实不是"**：
1. 终态要看 `agent_run.is_terminal`，**不能**看 `state.runtime_status` ——
   `runtime_status` 只在 FINISH 那条路径被 reducer 改成 FINISHED，
   预算耗尽走 `_declare_terminal` 不经过 Observation → State 仍写 RUNNING（→ 洞 238）
2. 预算取 `state.goal.budget.max_steps or config.max_steps`，**前者优先** ——
   只给 `AgentLoopConfig(max_steps=3)` 没用，`ScriptedInterpreter` 默认 6 会盖掉它

## M77 · 带着没被处理的失败，不许宣布完成（v2.1.65）

**探针实录（起因）**：
```
step 1  工具不存在 → StepOutcome.FAILED   agent_run running
step 2  脚本用完 → FINISH                 agent_run **completed**
```
而失败证据就在 `state.variables["result:exec_…"]` 里（`kind: execution_failed`），没人看。
**一个关键步骤失败了的 Run 被宣布 COMPLETED。**

**为什么是"说谎"不是"少功能"**：引擎看不见失败（按脚本走），
但**宣布终态的是 Runtime**（B-7）—— 谁宣布谁负责核实。

**新增 I-11**：完成前必须没有"**自上次规划以来**未被处理的失败"，有则 REPLAN。
判据钉在 **Loop 的 FINISH 分支**，不钉在引擎（换一个笨引擎谎言就回来了）。

**两个设计决定**（各有一条变红守着）：
- **按"上次规划"划界，不是数全部失败**：否则"第一步失败、换计划后成功"的 Run
  永远无法完成 —— 等于把可救的失败也判死刑。REPLAN 的语义就是"这份计划行不通"。
- **从 `state.observations` 推，不用计数器**：计数器在快照重载/进程重启时会丢，
  于是"有没有失败过"在重启前后给出不同答案。

**⭐ 它给 M76 修好的 REPLAN 边带来了第一个真正的触发条件** ——
M76 只是把边修通（通了、有界了），M77 才让它真的会被走。
两轮合起来：M76 修边 + M77 给触发条件，I-10 保证不空转（救不回来 → FAILED，
唯一不会变成的状态是 `completed`）。

**测试** 5 条（含两个控制组）。变红 2/2。单元 1233 / 集成 201 全绿。

**写测试踩到的**：`_loop_with_bad_tool` 放在第一个 TestCase 里，
第二个 class 用不到 → 直接内联构造 loop（别为了复用把 helper 挂到基类上）。

## M78 · 重规划必须真的换一份计划（v2.1.66）—— 补的是 M77 自己留下的洞

**探针**：M77 之后问"重规划换来的计划长什么样"：
```
#0: plan_id=plan_53a5…  shape=(('n0','step-0','task'), ('n1','step-1','task'))
#1: plan_id=plan_7bd2…  shape=(('n0','step-0','task'), ('n1','step-1','task'))
形状一样吗： True
```
`plan_id` 每次 `new_id()` → **从对象上看每次都"换了计划"**，但节点一个没变。

**⭐ 它把上一轮的 I-11 洗白了**（本轮最要紧）：
```
step 1  坏工具 → FAILED
step 2  I-11 拦下 FINISH → REPLANNED
step 3  重规划（形状相同）→ FINISH → **completed**
```
I-11 只看"上次规划以来"，重规划一次计数归零 →
**一个失败过的 Run 多花一轮重规划就又"成功"了**。

**新增 I-12**：换不出**形状不同**的计划 → 判 FAILED。
比形状不比对象（`plan_id` 必然不同，比对象 = 宣布"每次都换了"）。

## ⭐⭐ 元教训：一条不变量刚落地，就可能被它自己引入的机制绕过去

I-11 是上一轮写的，这一轮就被 I-11 自己的 REPLAN 洗白了。
**补完一条不变量之后，值得再问一句"它现在还能被绕过吗"。**
这一轮的答案：能，而且很容易。

## ⭐ 测试也要问"它到底在测哪条不变量"

I-12 加上之后，M76 那两条 I-10 用例会先被 I-12 判死 ——
于是**那两条用例测的是 I-12，不是 I-10**。改用 `VaryingPlanner` 才修好。
一条用例绿着，不代表它守着它以为自己在守的东西。

另一例：M78 的 `test_it_never_reaches_completed` 一开始没有
"必须在 3 轮内结束"这条断言，于是去掉 I-12 后它仍绿（靠预算兜底判死）——
**看起来在守着 I-12，其实什么都没守**。补上才真的红。

## ⚠️ M67 / M68：上一轮实现完了、没冻结（本轮补上）

**发现过程**：说"继续推进"后按惯例去基线找空洞，却发现**代码跑在文档前面** ——
`apps/migrate`（M67 生产迁移器）与 `apps/probe`（M68 进程探针）都已实现、
都有测试、都已接进 `deploy/k8s/*.yaml` 与 Dockerfile，
但基线停在 M66/v2.1.55，`_bootstrap.py` 里甚至已经有 `# M68：` 的注释。
本地记忆里**完全没有 M67/M68 的痕迹** → 上一轮是实现完就被打断了。

**⭐ 教训（与"写了没用上"并列的一类）**：**"实现了"和"冻结了"是两件事。**
本项目里程碑的完整性 = 空洞 → 实现 → 测试 → **变红验证** → **冻结基线**。
上一轮卡在第三步之前。以后接手先查：代码里的 M 编号 > 文档里的 M 编号吗？

**M67（迁移器）**：17 份迁移此前只有一个消费者（集成测试），
且用法是 DROP SCHEMA 后重放 —— 那不是迁移。`017` 是裸 ALTER，重放第二遍就
`column already exists`。新增 `apps/migrate` + 账本表 `agentos_schema_migrations`。
关键取舍 **O-1**：记"哪些已应用"，**不**把 SQL 改写成 `IF NOT EXISTS`
（后者篡改历史：已上线的迁移是不可变事实，改写 = 线上库与文件分叉）。
不变量 O-1~O-4（点名拒绝 drift / 迁移记账同事务 / 会话咨询锁）。

**M68（探针）**：要害是**两种红要分得开** —— 库挂了 ready 红 live 绿（摘流量别杀），
进程卡了才 live 红（重启）。合成一个结果 = 库挂时八个进程一起 CrashLoopBackOff。
O-5~O-9，其中 **O-9 是被真实 CrashLoop 打出来的**：阈值 30.0 与 sweeper 的
`max_idle_sleep` 30.0 **相等** → 空闲进程被判死 → 重启 → 又空闲 → 又判死，
日志全是 `signal ticks=N work=0`（正常退出的样子），**没有一行提到探活**。
取 90.0 = 3 倍最大退避，由测试**扫源码**盯着（`apps/*/app.py` 的 max_idle_sleep 全扫出来比大小）+ 控制组用例。

**变红验证**：M67 5/5（红 8 条）、M68 5/5（红 6 条）。

**踩到的坑**：变红脚本用 `subprocess(text=True)` 读子进程输出会
`UnicodeDecodeError`（Windows 控制台是 GBK）→ 必须 `encoding="utf-8", errors="replace"`。
计数正则原来写 `FAILED \((failures=(\d+), errors=(\d+))\)`，只匹配两者都出现的情况，
导致报"看不出来" —— 改成两段都可缺省。

**测试**：单元 1207 / 集成 201，全绿。服务 **v2.1.57**。

**新登记空洞 236**：`python -m apps.migrate` / `apps.probe` 只有 `__main__` 入口，
**没有 CLI 子命令**（与 M50 的九条不是同一套）。登记不治：
调用方是编排层（K8s / Dockerfile），不是人。

## 记忆文件到底存在哪（换账号时容易找不到）

用户问"上一个账号的 memory.md 在哪"。实测结论：

- **真正有内容的是项目级**：`C:\Users\19644\socialbook\agentos\.workbuddy-ai\memory\`
  —— `MEMORY.md`（长期笔记，13.6K）+ 按天日志 `2026-09-20/21/22.md`。
  换账号之所以还能接上，全靠这一层。
- **用户级** `C:\Users\19644\.workbuddy-ai\MEMORY.md` —— **不存在**（从未写过）。
- `C:\Users\19644\.workbuddy-ai\memory\<uid>_memory.md` 是**云端画像**，按账号 uid 分文件，
  本机有两个 uid（上一个 `fc4186a8…`、当前 `447fd036…`），**但 memoryBlock 全为空**，没有内容。
- **跨账号历史会话检索（conversation_search）两次都返回 0 条** —— 取不到上一个账号的对话。

⇒ 换账号后要还原进度，**唯一可靠来源是项目级 `.workbuddy-ai/memory/`**，别指望云端检索。

**已提示的两个风险**：
- GitHub 上 `dojoengine/asuka` 已存在（Rust agent framework，做游戏 NPC，末次提交 2025-02，活跃度低但同类同名）。建议加限定词：`asuka-kernel` / `asuka-runtime`。
- EVA 明日香的联想在中文技术圈极强，且角色特质（情绪化、崩溃）与"可靠执行内核"诉求相反。建议文档统一写「飛鳥 Asuka」把联想拉到飞鸟意象。


## M79 · 终态必须带上原因（v2.1.67）

**洞的形状**：`_declare_terminal` 是 FAILED 的唯一出口（S-7 刻意这么设计），
但四条不同的死法出去之后长得一模一样。探针实测：

    A 预算耗尽      run.finished | {'status': 'failed'}
    B 换不出新计划   run.finished | {'status': 'failed'}
    两份 payload 一样吗： True

**最刺眼的一点：自己打自己脸。** 那段 docstring 原话是
"trace 里的 run.finished 是"谁宣布了这个 Run 结束"的**唯一证据**" ——
而唯一证据里恰恰没有"为什么"这三个字。

**运维手上没有第二条线索**：`loop.history` 与 `last_outcome` 只在内存，
`RunSnapshot` 不带它们（`capture()` 里没有 history 字段），进程一死就蒸发。
账本（trace）才是 `GET /runs/{id}/trace` / CLI `trace` / 控制台 trace 视图
三处共同的唯一数据源。

**新增 B-12**：原因落在 `run.finished` payload 上。
关键不是"加个字段"，而是 `reason` 做成**必填关键字参数** ——
新增出口时不写原因就构造不出这次调用。
（默认值的诱惑：它让"忘了说为什么"看起来像"没什么可说的"。）

五个出口现在各自写明：预算耗尽 / 换不出新计划 / 连续被拒 / 外部叫停 / 目标达成。

**测试** `tests/unit/test_terminal_reason.py`（7 条）。
变红验证 3 变异全红（去 reason → 6 条；reason 变可选 → 1 条；预算原因改泛泛的 → 1 条）。
单元 1245 / 集成 201 全绿。服务 **v2.1.67**。

**顺带确认"被用上"**：控制台 trace 视图把 `e.payload` 渲染成 JSON，
CLI `trace` 直接 dump payload —— reason 真的会被显示，不是只写进账本没人看。

## M80 候选（**未动手**：探针写好但跑不了，环境故障）

**⚠️ 执行环境挂了**：Bash 工具连 `echo` 都返回 `-1058471934`，
PowerShell 能收 exit code 但拿不到 stdout，Python 起不来（写不出文件）。
绕过沙箱重试也无效 —— 是环境级故障，不是沙箱策略。
**恢复后第一步：跑 `probe80.py` 实证，再动手。**

候选洞：**子 Run 的终态事件不带 B-12 的原因给父 Run**（拟 **D-37**）。

读代码得到的链路（**尚未实证**）：

    _declare_terminal(status, reason=...)        ← B-12 已把原因写上
      → _emit_child_run_outcome(status)          ← 只传 status，**不传 reason**
      → 事件 payload["result"] = {status, steps, summary}
                                   ↑ summary = state.observations[-1].summary
      → ChildRunWaker._reason(handle) = f"child run failed: {summary}"

两条可疑之处：
1. `_emit_child_run_outcome` 手上就有 reason（它在 `_declare_terminal` 里），
   但没放进 `result`。
2. `_reason()` 拿**最后一条 observation 的 summary** 当理由 ——
   而预算耗尽**恰恰不产生 observation**（就是空洞 238 的形状）。
   于是父 Run 听到的是上一次工具调用的摘要，不是"预算耗尽"。

**为什么它有真实消费者**（这点比 238 强）：父 State 里那条
`child_run.finished` 的 `content["error"]` 是父 Agent 决定下一步怎么走时
唯一能看到的东西；I-12 要求重规划换一条路 —— **理由错了就换不对路**。

修法（小）：`_emit_child_run_outcome(status, reason=...)` → `result["reason"]`；
`_reason()` 优先用 `result["reason"]`，fallback 才用 summary。

**另一个被否掉的候选**：修 238（`state.runtime_status` 在四种终态下仍说 RUNNING，
取消也一样；`M15-核心领域模型.md:240` 还写着"与 AgentRun 状态保持同步" ——
五种只对了一种）。否掉的理由：`runtime_status` 今天是**只写不读**的字段，
修对它仍没人读，会踩"⭐ 交付的判据：被用上"那条。
将来若真有代码要读它（Intelligence 侧），应连同消费者一起补。

## 版本库：从"不是 git 仓库"到推 GitHub（Asuka）

用户要求：推到 `https://github.com/todochenxi/Asuka`（private），目录改名 `Asuka`。

**⚠️ 执行环境仍挂**（与 M80 那条同源，跨会话未恢复）：
Bash 工具任何命令都返回 `-1058471934`；PowerShell 能跑 cmdlet 但**原生进程
完全起不来**——`git --version` / `hostname.exe` / python 全部空输出且
`$LASTEXITCODE` 为空（说明根本没执行）。`Start-Process` 与 `cmd /c` 被安全策略拦。
`dangerouslyDisableSandbox` 无效。⇒ **git 命令只能由用户自己敲**。

**实证结果（PowerShell cmdlet 可用，故可查）**：
- `C:\Users\19644\socialbook\agentos` **没有 `.git`**，不是 git 仓库 → 需 `git init`
- 无 `.gitignore`（已新建，是本次唯一新增文件）
- 31 个 `__pycache__/` 目录、`.workbuddy-ai/`（0.25MB）、`*.log`
- `gh` CLI **未安装**；`git` 在 `C:\Program Files\Git\cmd\git.exe`
- SSH 公钥已有：`~/.ssh/id_ed25519.pub`（comment `tommy`）
- 用户明确说**什么都不用删**（`probe80.py` / `p80.txt` / `_t.py` 等调试残留全部保留）

**给用户的关键提醒**：
1. GitHub 网页先建空仓库 **Asuka（Private）**，**不要勾选** README/.gitignore/license，否则首推冲突
2. HTTPS 推送不能用账号密码，要 **PAT（scope: repo）**；或改走 SSH
   （`git@github.com:todochenxi/Asuka.git`，公钥已存在）
3. ⚠️ **只改目录名，不要批量替换内容里的 `agentos`** ——
   包名 `packages/agent_*`、模块 `apps.*`、文档标题都是内容，
   批量替换会打红 1200+ 测试
4. `deploy/k8s/02-secret.yaml` 含明文 demo 口令 `password: "agentos"`
   （文件头已写明上线前用 `kubectl create secret` 替换）
5. 改名要在**父目录**执行，且无进程把该目录当工作目录

（后记：推送成功，commit `6403400`，远端 SSH 形式。
踩到的两个坑：① `git init -b main` 对**已初始化的** repo 不改分支 → 停在 master →
`src refspec main does not match any`，用 `git branch -M main` 修；
② Windows 上 `failed to execute prompt script` = Git Credential Manager 弹不出窗，
绕过去即可（SSH 或 PAT 塞 URL）。**目录尚未改名。**）

## M80 · 子 Run 的死因必须跟着结果一起交给父 Run（v2.1.68）

**⚠️ 执行环境在这一轮中间恢复了**（此前 Bash 任何命令返回 `-1058471934`、
PowerShell 原生进程完全起不来）。恢复后第一件事就是跑 `probe80.py` 实证 ——
**结果是对的：先盲写的那个探针有个不存在的 import**（`packages.execution_kernel.tool`），
修掉才跑起来。**环境故障期间写的代码一律要当"未验证"处理。**

**探针实录（洞的形状）**：

    === A 子 Run 预算耗尽 ===
      子 Run 真因        step budget exhausted (2/2)
      父 Run 会听到的    child run failed: execution exec_xxx attempt#1
                         COMPLETED (completed)
    === B 子 Run 换不出新计划 ===
      子 Run 真因        replan produced a plan with the same shape ...
      父 Run 会听到的    child run failed: plan invalidated; replanning

**⭐ 不是说漏了，是说反了。** `summary` 是"最后一条 observation"，是**过程**不是**结论**；
而预算耗尽恰恰不产生 observation（就是空洞 238 的形状），
所以最后那句自言自语是**上一步**的，方向完全相反。

**为什么它比 238 强**：有真实消费者 —— `_reason()` 的产物就是父 State 里
`child_run.finished` 的 `content["error"]`，是父 Agent 决定下一步的唯一线索；
I-12 要求重规划换一条路，**理由错了就换不对路**。238 是只写不读的字段，所以被否掉。

**新增 D-37**：子 Run 交给父 Run 的失败原因，必须是子 Run 真正的死因。
- 发射侧 `_emit_child_run_outcome(status, *, reason)` —— **必填关键字参数**（与 B-12 同款）
- 消费侧 `_reason()` 优先用 `result["reason"]`
- ⭐ **死因缺失时说"没记录到"，不许拿 summary 顶替** ——
  落库早于 D-37 的那批 result 没有 reason，把 summary 当死因就是**编造**，
  而它恰恰可能是反的。宁可少说，也要说清那句话是什么性质的。

**测试** `tests/unit/test_child_failure_reason.py`（9 条）。
⚠️ 关键一条特意加了控制组：断言"父 Run 听到的不是执行摘要"的同时，
必须断言 `summary` 里**确实**写着 COMPLETED ——
否则两者恰好一样时那条会假绿，而它值得断言恰恰因为它们是**反**的。

**变红验证 4/4 全红**：去 reason → 4 条；reason 变可选 → 1 条；
`_reason` 退回用 summary → 5 条；缺失时把 summary 当死因 → 2 条。
（第一轮 M1 因锚点 `"reason": reason,` 在 loop.py 里出现 4 次被 SKIP，
加上下文变成唯一才跑成 —— **变红脚本要断言"命中次数==1"**，否则静默跳过。）

单元 1245 → **1254**；集成 **201**（venv python）。全绿。

**新登记空洞 239**：子 Run 的 `result` 是自由 JSON，`reason` 无 schema 约束，
D-37 靠调用约定成立不是靠结构。登记不治（加 CHECK 影响历史行 + 牵动迁移），
`_reason` 侧已有诚实降级。

## M81 · 委派失败了的父 Run，不许宣布完成（v2.1.69）

**起因**：M80 冻完后按 §0.4 那条回头问"**刚落地的 I-11 能不能被绕过**"。

**探针实录（`probe81.py`）**：

    step 2  子 Run 失败交回（父 Run 那条委派 Execution 真的进了 FAILED）
    父 State: plan.created / child_run.spawned / child_run.finished
    I-11 判据 _failures_since_last_plan() = 0        ← 数不到
    step 3  FINISHED → completed
    账本     {'status': 'completed', 'reason': 'goal reached'}

**形状：同一个事实，两条路给出两个答案。** 正常执行失败时 Loop 的 `_record()`
会写 `execution.failed`；委派这条路径**不经 Worker，没人写** ——
State 上只有 `child_run.finished`（那说的是"子 Run 完事了"，是另一件事）。
I-11 从 State 读，于是读不到。从 M77 到 M80 整整四轮没人发现。

**新增 I-13**：委派失败就是一次执行失败，必须进 State（`execution.failed`，
且绑定真实 Execution —— 用 `Observation.from_execution_result` 而非手搓，I-6）。
**只治 `failed`**：`cancelled` 不是失败（S-15），`unknown` 是不知道（D-19）。

**⭐⭐ 测试设计上栽的一处（本轮最值钱）**：
第一版断言"最终状态不是 completed" → **红了**。因为 VaryingPlanner 下
委派失败 → REPLAN（新形状）→ 判据归零 → FINISH → **completed**，
而**那是 I-11 设计允许的**（失败可以被救回来，M78 用 I-12 堵住了洗白）。
⇒ I-13 真正的可观测后果不是"最终不能完成"，而是
**不能从委派失败直接走到 FINISH，中间必须隔着一次 REPLAN**。

**另一处**：新计划是在 REPLANNED 的**下一步**才落到 State 的
（`plan.invalidated` 与 `plan.created` 分两步写），
所以"判据归零"不能在 REPLANNED 当场断言。

**测试 8 条** `tests/unit/test_delegation_failure_blocks_completion.py`。
**变红验证 4/4 全红**：整块失效→5；kind 改 EXECUTION_RESULT→5；
取消也算→1；绑错 execution→1。
单元 1254 → **1262**；集成 201。全绿。

**⭐ 元教训（值得单记）**：**判据读什么，决定了它能看见什么。**
I-11 判据写的是"数 `execution.failed` observation" —— 这句话本身没错，
但它把"什么算失败"**外包给了写 observation 的人**。
谁忘写，那条失败就从判据视野里消失，而判据自己毫不知情。
⇒ 补完一条不变量后，除了问"还能被绕过吗"，还要问：
**它依赖的那些事实，是不是每一扇门都在写？**

## M82 · 委派没有回音的父 Run，不许宣布完成（v2.1.70）

**起因**：M81 只治了 `failed`，留着 `cancelled` / `unknown` 没治。
按 §0.4 回头查 `unknown` 那扇门（`child_wait_expired`）：

    kernel.status_of(父委派 Execution) = FAILED      ← 确实被判死了
    State: plan.created / child_run.spawned / child_run.unknown
    I-11 判据 = 0  →  step 3 FINISHED → completed / goal reached

**与 M81 一字不差的同一句话，换了一扇门进来。**

**⭐ 难点：两个现成选项都不对** ——
写 `execution_failed` 违反 PR-19（那条子 Run 可能正在别的 worker 上跑得好好的，
"判死"是**我们不再等**不是"它做不成"，D-19）；不写就是上面这一幕。
⇒ **两个选项都不对时，缺的是第三个 kind。**

**新增 I-14**：
- `reducer.py` 新增 `EXECUTION_UNRESOLVED = "execution_unresolved"`
- 它必须进 State，但**不许冒充失败**
- I-11 判据扩成"**这一步没有被证明成功**"（`execution_failed` + `execution_unresolved`）
- `cancelled` 仍不在此列：S-15 说取消不是失败，且它是**父侧主动的选择**
  （`failed` 是子 Run 判的 / `unknown` 是我们判的 / `cancelled` 是父侧判的，
  只有前两种是"父 Agent 不知道的坏消息"）

**测试 10 条** `tests/unit/test_unresolved_delegation_blocks_completion.py`。

**⭐⭐ 变红验证第一轮 M4 没红 —— 漏在测试自己**（与 M75 同一个坑）：
M4 = "reducer 不认这个新 kind"，预期红，结果 **0 条**。
根因：**`reducer.py` 末尾有兜底分支** ——

    state.variables[f"obs:{obs.observation_id}"] = obs.summary   # 任何 observation 都留摘要

所以"新 kind 没被认"时那条 observation 照样**存在**于 State 上，
`assertIn(kind, kinds)` 照样绿。**把"存在"当成了"被处理"。**
真正能区分的是"它有没有把这一步当结束了" → 断言
`execution_id in loop.state.completed_tasks`。补上后 M4 红了 2 条。

**最终变红 5/5 全红**：不进 State→9；冒充 failed→4；判据退回→3；
reducer 不认→2；摘要说 failed→1。单元 1262 → **1272**；集成 201（有一条偶发红，
重跑全绿；按记忆里那条"偶发红必须完整捕获输出落盘"处理的）。

**⛔ 留了一个洞没治（登记候选 240）**：`cancelled` 这条路径
—— 父 Run **不**产生任何 State 痕迹（`child_run.finished` 的 outcome
= "cancelled"，I-11 判据读到 0），于是"父 Run 放弃等待、把子 Run 取消掉"
之后，它可以宣布 completed。我判断不算说谎（S-15：取消是父侧主动选择，
父 Run 自己知道），但**没做实证**，所以在 §119 里写明这个判断及其理由，
不声称已验证。

## M83 · 取消不是"什么都不知道"（v2.1.71）—— 零代码改动的一轮

**起因**：M82 的文档对 `cancelled` 只留了一句**没有实证的判断**
（"父侧主动选择，不构成被隐瞒的失败"）。**判断不能当结论用。**

**探针（`probe83.py`）分两件事测**：

    case A  父 Run 等不下去，自己取消子 Run
    case B  子 Run 被别人取消，父 Run 只是收到通知

实测 **A 和 B 留下的记录完全一样** —— 因为**能走到这里的只有 A**
（`child_cancelled()` 的调用者是父侧 waker）。"被动得知"是
**还没有实现的能力**，不是正在说谎的既有路径。

⇒ **这恰恰说明不该区分它们。** 第一反应可能是"补个字段分开"，
但那会造出一个**谁也填不了的维度** —— 账本上每一列都该对应一条真实路径。
（这是另一种方向的不诚实：为想象中的系统写代码。）

**新增 I-15**：三个终态在父侧的**信息量**不同，必须分得开：
- `failed` → `child_run.finished` + `execution_failed`（I-13）：它失败了，因为什么
- `cancelled` → `child_run.finished`（outcome + error 都在）：**为什么**被取消
- `unknown` → `child_run.finished` + `execution_unresolved`（I-14）：**什么都不知道**

守两条：① 取消必须在父侧留下**可读的原因**；② 取消**不能被记成"不知道"**
（那是 D-19 的形状，会抹掉已知的原因）。

**测试 13 条** `tests/unit/test_cancellation_is_not_ignorance.py`。
**变红 4/4 全红**：不写原因→2；记成 failed→2；记成 unresolved→3；
summary 不说 cancelled→2。

**⭐ 本轮 `packages/` 一行没改** —— 13 条测试全绿通过、4 条变异全红，
说明既有行为本来就是对的，缺的只是"有东西守着它"。
这正是 §0.5 那一类值得单独开一轮的里程碑：
把它从"读过代码觉得对"变成"有变异守着的结论"。

**⛔ 这里也登记一个真正的空洞（候选 240/241）**：
"子 Run 被别人取消 / 被系统取消，父 Run 事后才知道"是**未实现的能力**，
而且现在**没有登记任何地方**。将来若要实现，父侧 State 必须新增一个
能表达"我是被动得知的、且不知道上游意图"的形状 ——
现在 `outcome='cancelled'` 这一个值承担不了两种语义。

## ⚠️ 两个 python 不是同一个（本轮新踩）

系统 python 没装 `psycopg` / `httpx`。用它跑集成 → `FAILED (errors=1, skipped=188)`
—— 188 条**全被跳过**，看起来像"只有 1 条红"，其实**一条都没真跑**。
集成必须用 `C:/Users/19644/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe`。
（那条 `errors=1` 是 `test_api_real_http` 缺 httpx 的 import 错，也不是代码问题。）

---

# M84（v2.1.72）· 那条"偶发红"根本不是偶发

## 结论先写

**它不是偶发，是 M66 已经诊断过、却没写进基线的旧账。**
唯一会红的那条是 `TheProcessServesTest.test_the_version_it_serves_is_the_frozen_baseline`，
报 `'2.1.x' != '2.1.y'`。

## 排查路径（值得复用）

1. **先撞**：按纪律完整落盘（`> 文件 2>&1`，**不用 `tail`**）。
   - 9 次串行 + **4 次并发双跑** = 13 次、2613 条用例，**一次不红**。
   - 并发那 4 次很值：B 耗时是 A 的两倍多（275s vs 122s）→
     **PG 咨询锁确实在排队**，`_pg.py` 的设计生效，"两进程拆台"排除。
2. **撞不出来就换方向：翻任务账本**（`TaskList` / `TaskGet`）。
   → 任务 **#103** 就是答案，连测试名都点出来了。
   ⭐ **这一步是整轮的关键。撞不出来不等于问题不存在，
   可能只是它的触发条件不在"跑测试"这个动作里。**

## 形状

升版要动**三处**（这是"多步手动动作"）：

| 落点 | 内容 |
|---|---|
| 基线文档名 | `AgentOS_…_v2.1.72_最终冻结版.md` |
| `apps/api/app.py` | `FastAPI(version="2.1.72")` |
| `deploy/k8s/*.yaml` | `agentos:2.1.72-b1`（14 处） |

而集成那条断言是**跨文件**的：
- 左边 = 真 uvicorn 答的 `/openapi.json` ← `app.py`
- 右边 = `_frozen_baseline_version()` ← 文档**文件名**

**停在两步之间跑测试就红。** 它"偶发"只因"停在两步之间"是手动动作的时间窗。
**被测代码一秒都没坏过** —— 那次红灯指向了一个不存在的问题。

## 落地（PR-33）

新增两条**不依赖 PG** 的单测（`tests/unit/test_http_api.py`）：

- `test_the_version_bump_is_atomic_across_all_its_landing_sites`
- `test_the_k8s_image_tags_follow_the_frozen_version`

两个设计决定：
1. **只认"声明点"，不扫全文。** 基线文档正文有整节「版本变更」列着历代版本，
   扫全文会把**历史记录**判成**漂移** → 永远红的噪声。
2. ⭐ **声明点在哪一层，就在哪一层读它。** 写测试时我自己踩了一次：
   文档的声明在**文件名**上，我却对它 `read_text()` → 永远匹到 0 次。
   **把"存在"当成了"被处理"**，与 §119.5 是同一种错换了个位置。

变红验证 3/3（app.py 漂移 / k8s 一处 tag 漂移 / 文档名漂移），恢复后全绿。
单测 **1285 → 1287**；集成 **201** 不变。

## ⭐ 本轮最值钱的一句教训

**诊断也是有归宿的。「写进任务卡」≠「写进基线」。**

一句正确的结论如果没落进"下一个人一定会读到的地方"，就等于没做过。
任务卡是**过程**，基线是**结论**；把结论留在过程里，
下一个接手的人得到的不是"已知问题"而是"疑似新问题" ——
他会再花一遍同样的时间，再得到一遍同样的结论。
**这比红灯本身贵得多。**

⚠️ **接手项目时该加的一条检查**：不只是查"代码里的 M 编号 vs 文档里的 M 编号"，
还要查 **「已诊断但未固化的结论」** —— 把已完成任务的 description 扫一遍，
看有没有"根因是 X"却**没有对应测试**的条目。

## 排查纪律（本轮再次验证）

1. **证据完整落盘**，别让 `tail` 吃掉失败名。上一轮定位不到就因为这个。
2. **撞不出来 → 翻账本**，别继续空跑消耗预算。
3. 用**正确的解释器**：本项目是 `unittest`（不是 pytest），
   且集成必须用 `C:/Users/19644/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe`
   （系统 python 缺 psycopg/httpx → `skipped=188`，**一条都没真跑**）。
