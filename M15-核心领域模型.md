# M15 核心领域模型 · Kernel Implementation 记录（对齐基线 v2.1.16）

> 架构冻结后的第一件事：**不是 FastAPI，也不是 Kafka**。
> 先把 8 个核心对象的领域模型和不变量定义出来 —— 这一步做好，PG 表、API、Worker、Kafka 会自然长出来。

> **基线**：`AgentOS_企业级Agent平台架构_v2.1.16_最终冻结版.md`（唯一架构事实源）。
> 本文件是基线的代码级展开，冲突时以基线为准。
> 本轮同步点：`Task:Execution = 1:1`、Lease 挂 Execution + `fencing_token`、
> `idempotency_key = execution_id`、Checkpoint 拆 Kernel/Run 两层、State 并发串行化、Execution 新增 `STALE`。

> **v2.1.2 回写说明**：M15 阶段 1~8 写完后，本文件中的以下结论已反向写入基线 §50——
> Action 补 `LLM_CALL`（否则模型调用绕过 Harness）、Kernel 补 `AttemptRepository`（否则重载后拒绝成功）、
> 取消补 `sweep()` 第四段（否则取消永久悬空）、Recovery 补 PG 安全网（否则 Redis 全丢漏扫）、
> 事件去重落 PG（否则成为"丢一次错一次"）、Worker 补 `LOST_LEASE`、Scheduler 过滤取消意图、
> State 补 `reducing()` 写窗口、Task 补 `ExecutorType`。详见基线 §49/§50。

> **v2.1.3 回写说明**：阶段 10 补上 Business Domain 后又有三条结论反向写入基线 §52——
> **B-7：Run 的终态只能由 Runtime 声明，不能从 Step 派生**（否则"Step 全绿"会被当成"Run 完成"，
> Runtime 想标 FAILED 时被终态不可变顶回来）；**§10 补 `CREATED → SUSPENDED` 这条边**
> （Run 的第一个动作就被闸门挡住时，Run 从未 RUNNING 过）；**Step:Task 必须显式传 `step_id`**
> （否则 1:N 静默退化成 1:1）。
>
> **v2.1.4 回写说明**：阶段 11 把 §44 第一条 End-to-End 闭环接通后，又有一批结论反向写入基线 §53——
> **L-3：Token 用量必须回流 CostManager**（否则 `Budget.max_tokens` 接了没通电）；
> **L-7：连续被 DENY 到上限必须判 Run FAILED**（被拒既不花钱也不占步数，否则永远空转且**没有任何报错**）；
> **G-8：缺省模型由 Gateway 决定**（Executor 是翻译层，不拥有模型知识）；
> **C-11：Context 组装归 Runtime、准入归 Harness**（§23 的 `ContextManager` 指后者）；
> **C-4：Context 截断必须记录**（不记录 = 让模型在不知情的前提下失明）。
>
> **v2.1.6 回写说明**：M18 补上 Control Plane 契约层后，又有两条结论反向写入基线 §55——
> **A-9：过期 ≠ 已决定**（EXPIRED → 410，而不是 409 `APPROVAL_ALREADY_DECIDED`；
> 后者会告诉调用方"有人批过了，去等结果吧"，而那个结果永远不会来）；
> **A-10：待审批列表查存储不查内存 Loop**（否则服务一重启列表就空，而 Run 还挂着等人批：
> 界面上什么都没有，系统里全在等）。
>
> **v2.1.7 回写说明**：M19 把审批落进 PG 后，又撞出一条结论反向写入基线 §56——
> **A-11：审批的判定与写入必须是一次原子操作**。在此之前"先读一下状态再写"被当成正确的并发写法，
> 但它只在**顺序**调用下成立：两个审批请求同时到达时，两边读到的都是 PENDING，
> 前置检查都通过，后写的覆盖先写的，"谁批的"变成后到的那个人。
> 配套 **A-12**（存储选型的判据是"丢了之后变慢还是变错"，不是"重不重要"）与
> **A-13**（审计约束下推到 DB：领域检查是善意，DB 约束才是兜底）。
>
> **v2.1.8 回写说明**：M20 把"重启后批准"接通后，撞出的**两条缺陷都不是 Recovery 自己的问题** ——
> **X-13：跨 Port 拿到的是副本，不是同一个对象**（`bind()` 改的是存储里那条，
> 手上那个对象不会跟着变；内存版因为 `get()` 返回同一对象而"碰巧正确"，换成 PG 版就静默失效）；
> **E-25：乐观锁比的是"上次存储边界"的版本，不是"上次内存自增前"**
> （`resume()` = SUSPENDED→PENDING→RUNNING 两次跃迁却只落一次库，
> 于是在**根本没有并发**的情况下自己报 E-13）。配套新增 **R-1 ~ R-5**。
>
> **v2.1.9 回写说明**：M10（Saga / Compensation）把"外部副作用"纳进账本后，
> 又有一条结论反向写入基线 §58 ——
> **S-9：批准一个动作 = 同时批准它的撤销**。
> 它一开始只是注释里的一句断言，而撤销阶段是**故意绕过** `before_action` 的
> （否则"人走了、Run 失败了"会让副作用永久留着，且不报错）。
> 既然撤销时不再审一次，"一起审过"就必须发生在**正向动作提出时** ——
> 本轮把它落成了 `Harness.before_action` 里对逆操作的一次真实 Policy 评估。
> 配套新增 **S-1 ~ S-16**，其中 **S-16** 是写测试时才发现的：
> 成功的 Run 会在账本里留下一串"待撤销"（指向错的假待办），必须结案为 `NOT_NEEDED`。
>
> **v2.1.10 回写说明**：M21 新建 `apps/` 进程层后，又有三条结论反向写入基线 §59 ——
> **PR-11：带内部节奏的对象必须跨 tick 存活**。`RecoveryController.sweep_every` 的
> 节奏计数在控制器对象里，每轮重建则 `ticks` 永远是 1，`ticks % N` 永远不是 0，
> **PG 安全网一次都不会触发** —— 兜底路径变成永远不会执行的死代码，
> 而它正是"Redis 全丢也只允许变慢"的兑现方式。
> **PR-12：收敛型进程不需要领地**。判据不是"要不要多副本"，而是
> "这个动作重复做会不会改变结果"：投两次有两封，`cancel` 两次还是一次。
> **PR-13：唤醒谓词必须每轮现取审批结果** —— A-10 第三次以同一副面孔出现，
> 而这次最隐蔽，因为"构造时快照一次"看起来完全合理，
> 且所有单 tick 内构造+断言的单元测试全部通过。
> 配套新增 **PR-1 ~ PR-13**，以及 `006_outbox_delivery.sql`（投递领地）。

```
Goal / State / Decision / Action / Observation      ← Intelligence Contracts
AgentRun / Step                                     ← Business Contracts   （阶段 10 补齐）
RunTrace                                            ← Runtime 审计账本      （阶段 11 补齐）
Task / Execution / Attempt                          ← Execution Contracts
```

外加支撑对象：`Lease` / `KernelCheckpoint` / `RunCheckpoint` / `Cancellation` / `Event`

---

## 0 · 设计原则

1. **纯 Python，零外部依赖**（`packages/agent_domain`，不 import fastapi / redis / kafka）
2. **不变量写在对象内部**，不是写在调用方 —— 违反即抛异常
3. **状态不可直接赋值**，必须走 StateMachine 的 `transition()`
4. **每个对象带 `version`**，用于 Optimistic Lock
5. **ID 用带前缀的字符串**（`run_xxx` / `task_xxx` / `exec_xxx`），方便日志排查
6. **同一 run 的 `State.apply()` 必须串行化**（version 校验 + 冲突重放），并行 Task 不得并发写 State
7. **写回必须带 `fencing_token`**，过期持有者的写入一律拒绝

---

## 1 · 对象全景与归属

```
                        ┌─────────────────────────────────┐
                        │      Intelligence Contracts     │
                        │  Goal  State  Decision          │
                        │  Action  Observation            │
                        └───────────────┬─────────────────┘
                                        │  Action → TaskFactory
                        ┌───────────────▼─────────────────┐
                        │      Execution Contracts        │
                        │  Task  Execution  Attempt       │
                        │  Lease  Checkpoint(Kernel/Run)  │
                        └─────────────────────────────────┘
```

| 对象 | 层 | 谁创建 | 谁消费 |
| --- | --- | --- | --- |
| Goal | Intelligence | Goal Interpreter | Decision Engine |
| State | Intelligence | Runtime | Decision Engine / Context Engine |
| Decision | Intelligence | Decision Engine | Action Resolver |
| Action | Intelligence | Action Resolver | Task Factory |
| Observation | Intelligence | Observation Processor | State / Context |
| **Task** | **Kernel** | Task Factory | Scheduler |
| **Execution** | **Kernel** | Kernel（受理 Task） | Worker |
| **Attempt** | **Kernel** | Execution | Worker |
| Lease | Kernel | Worker（claim **Execution** 时） | Recovery Controller（扫描过期） |
| KernelCheckpoint | Kernel | Worker | Recovery / 新 Attempt |
| RunCheckpoint | Runtime | Runtime（Step 完成时） | Harness / 断点续跑 |
| **Step** | **Business Domain** | **Runtime**（Planner 产出后实例化） | Task Factory（派生 Task） |

> **Step 属于 Business Domain，不属于 Kernel。** 它是 Plan Node 的运行实例，Kernel 只看到它派生出的 Task。

---

## 2 · Intelligence Contracts

### 2.1 Goal

```python
@dataclass(frozen=True)
class Goal:
    goal_id: str
    run_id: str
    objective: str                    # "分析 Q3 财务风险"
    constraints: list[str]            # ["只能使用内部数据"]
    success_criteria: list[str]       # ["收入分析", "现金流分析", "风险分析"]
    priority: int = 0
    deadline: timedelta | None = None # 10m
    budget: Budget | None = None      # max_cost=1.0, max_tokens=...

@dataclass(frozen=True)
class Budget:
    max_cost: float | None = None
    max_tokens: int | None = None
    max_steps: int | None = None
    max_duration: timedelta | None = None
```

> **Goal ≠ UserRequest**。用户说"帮我看看 Q3 财务怎么样"是 UserRequest，
> 经 **Goal Interpreter** 转换后才成为系统可执行的 Goal。

**不变量**
- `objective` 非空
- `success_criteria` 至少一项（没有判据就无法评估 Outcome）
- `budget` 一旦设定，运行期只能收紧不能放宽

---

### 2.2 State

```python
@dataclass
class State:
    run_id: str
    goal: Goal
    current_plan: Plan | None = None
    active_tasks: list[str] = field(default_factory=list)
    completed_tasks: list[str] = field(default_factory=list)
    observations: list[Observation] = field(default_factory=list)
    variables: dict[str, Any] = field(default_factory=dict)
    constraints: list[str] = field(default_factory=list)
    runtime_status: str = "RUNNING"
    version: int = 1                  # Optimistic Lock

    def apply(
        self,
        obs: Observation,
        reducer: "StateReducer",
        expected_version: int | None = None,
    ) -> None:
        """
        唯一的状态更新入口。

        Observation 不直接改 State —— 必须经过 Reducer 解释：
            Execution Result → Observation（事实）→ StateReducer（解释）→ New State

        并发语义（并行 Task 必须遵守）：
            - 同一 run_id 的 apply 必须串行化（per-run 有序队列）
            - expected_version 不为空时必须等于当前 version，否则抛 ConcurrentStateError
            - 冲突时由调用方重读最新 State 并重放 Reducer，不允许覆盖写入
        """
        if expected_version is not None and expected_version != self.version:
            raise ConcurrentStateError(
                f"state version mismatch: expected={expected_version}, actual={self.version}"
            )
        self.observations.append(obs)          # 事实：append-only
        reducer.reduce(self, obs)              # 解释：写入 variables / completed_tasks
        self.version += 1

class StateReducer(Protocol):
    def reduce(self, state: "State", obs: Observation) -> None: ...

class ConcurrentStateError(Exception):
    """State 并发写冲突：调用方应重读最新 State 并重放 Reducer，不允许覆盖。"""
```

> State 分四种形态，不要全塞进 Prompt：
> `Durable State(PG)` / `Runtime State(Redis)` / `Context(给模型)` / `Memory(跨 Run)`

### ⚠️ Observation **不直接**更新 State

```
Execution Result
      ↓
Observation Processor
      ↓
Observation                ← 事实："balance = 100"
      ↓
State Transition (Reducer) ← 解释：写入 variables
      ↓
New State                  ← "account_balance = 100"
```

| | 性质 | 例子 |
| --- | --- | --- |
| **Observation** | 事实 / 感知（append-only，不可变） | Tool 返回 `balance = 100` |
| **State** | 系统解释后的当前状态（可变，带 version） | `variables["account_balance"] = 100` |

> 少了 Reducer 这层，Observation 和 State 又会重新混起来。

**不变量**
- 只能通过 `apply(observation, reducer)` 变更，**禁止外部直接改字段**
- `observations` 只增不减（append-only 事实流）
- `completed_tasks` 只增不减
- `runtime_status` 与 AgentRun 状态保持同步（由 Kernel 事件驱动）
- **同一 run_id 的 `apply()` 必须串行化**：并行 Task 的 Observation 走 per-run 有序队列，禁止并发写入
- `expected_version` 不匹配时重读 + 重放 Reducer，**不允许覆盖**
- Kernel 的 OCC 管"数据库并发写"，State 的 `version` 管"语义并发写"，两层都要有

---

### 2.3 Decision

```python
@dataclass(frozen=True)
class Decision:
    decision_id: str
    run_id: str
    step_seq: int
    action: Action                    # 选出的动作
    evidence: list[str] = field(default_factory=list)   # 推理依据
    confidence_signal: float | None = None   # ← 注意：不是概率
    decision_metadata: dict[str, Any] = field(default_factory=dict)
    reasoning: str | None = None
    created_at: datetime = field(default_factory=now)
```

> ⚠️ **不要用 `confidence` 这个名字，也不要当概率用。**
> `{"action": "send_email", "confidence": 0.97}` **不意味着决策 97% 正确** —— LLM 给出的 confidence 不是 calibrated probability。
> - 字段命名：`confidence_signal` 或归入 `decision_metadata`
> - **禁止** `confidence > 0.9 → 自动执行` —— 这是危险设计
> - 正确用法：作为**排序 / 人工复核优先级**信号；是否自动执行由 **Policy / Guardrail / 风险分级**决定

> **Decision 不直接执行。** Decision 属 Intelligence，Execution 属 Runtime，必须隔离。
> 正确链路：`Decision Engine → Decision → Action Resolver → Action → TaskFactory → Task → Kernel`

**不变量**
- `confidence_signal ∈ [0, 1]`，可为 `None`
- `action` 必填（Decision 若不产生 Action，应显式为 `Finish`）
- Decision 是不可变的（frozen）—— 它是历史事实，要能 Replay

---

### 2.4 Action

```python
class ActionType(str, Enum):
    TOOL_CALL       = "tool_call"
    SKILL_CALL      = "skill_call"
    AGENT_DELEGATION= "agent_delegation"
    HUMAN_APPROVAL  = "human_approval"
    ASK_USER        = "ask_user"
    FINISH          = "finish"

@dataclass(frozen=True)
class Action:
    action_id: str
    action_type: ActionType
    payload: dict[str, Any]           # 各类型各自的载荷
```

| ActionType | payload 内容 |
| --- | --- |
| `TOOL_CALL` | `{ tool_name, tool_version, arguments }` |
| `SKILL_CALL` | `{ skill_name, skill_version, input }` |
| `AGENT_DELEGATION` | `{ target_agent_id, goal, context_boundary }` |
| `HUMAN_APPROVAL` | `{ prompt, approvers, timeout }` |
| `ASK_USER` | `{ question, options }` |
| `FINISH` | `{ result, artifact_refs }` |

**不变量**
- `HUMAN_APPROVAL` 必须带 `timeout`（否则可能永久挂起）
- `AGENT_DELEGATION` 必须带 `context_boundary`（Agent 间不共享数据库）

---

### 2.5 Observation

```python
@dataclass(frozen=True)
class Observation:
    observation_id: str
    run_id: str
    source: ObservationSource         # TOOL / LLM / HUMAN / CHILD_AGENT / EXTERNAL
    source_id: str                    # tool_call_id / child_run_id / ...
    content: list[ContentBlock]
    artifact_refs: list[ArtifactRef] = field(default_factory=list)
    created_at: datetime = field(default_factory=now)

@dataclass(frozen=True)
class ArtifactRef:
    artifact_id: str
    type: str                         # pdf / xlsx / png / json
    uri: str                          # s3://agentos/artifacts/xxx.pdf
    metadata: dict[str, Any] = field(default_factory=dict)
```

> **大文件绝不塞进 Observation。** 走 `Execution → Artifact → S3`，Observation 只存引用。

**不变量**
- Observation 只能由 **Attempt 的真实执行结果**产生，不可凭空构造
- `content` 非空 或 `artifact_refs` 非空（至少要有一样）
- 不可变（frozen）—— 是历史事实

---

## 3 · Execution Contracts

### 3.1 Task（可调度工作单元）

```python
@dataclass(frozen=True)
class Task:
    task_id: str
    run_id: str
    step_id: str                      # 溯源到哪个 Step
    task_type: str                    # tool_call / llm_call / skill_call / ...
    executor_type: str                # native / mcp / http / agent_runtime / workflow
    payload: dict[str, Any]
    priority: int = 0
    resource_requirement: ResourceReq = field(default_factory=ResourceReq)
    retry_policy: RetryPolicy = field(default_factory=RetryPolicy)
    timeout: timedelta = timedelta(seconds=60)
    tenant_id: str | None = None
    version: int = 1
```

> **Scheduler 只认识 Task。** 它不知道什么是 Agent、什么是 Step。
>
> ⚠️ **Idempotency Key 不在 Task 上。** 它挂在 **Execution**（`key = execution_id`），
> 由 Kernel 在受理 Task 时生成，**跨 Attempt 保持稳定** —— 每次重试换新 key 就完全失去防重复的意义。

**关系基数（对齐基线 v2.1.1）**

```text
AgentRun  : Step      = 1 : N
Step      : Task      = 1 : N
Task      : Execution = 1 : 1     ← 重跑必须新建 Task，不能在同一 Task 上开第二个 Execution
Execution : Attempt   = 1 : N
```

**不变量**
- `step_id` 必填 —— 保证每个 Task 可溯源到逻辑节点
- `executor_type` 必须是已注册值
- **一个 Task 最多对应一个 Execution（1:1）**；需要重跑时必须新建 Task

---

### 3.2 Execution（Kernel 生命周期实体）

```python
@dataclass
class Execution:
    execution_id: str
    task_id: str                      # 1:1，唯一索引
    idempotency_key: str              # = execution_id，跨 Attempt 稳定（Retry ≠ Safe 的唯一防线）
    execution_mode: str = "task"      # task（无状态短任务）/ stateful（长任务 Actor）
    status: ExecutionStatus
    parent_id: str | None = None      # 父 Execution（形成 Trace Tree）
    current_attempt_no: int = 0
    lease: Lease | None = None        # ← Lease 挂 Execution，不挂 Task
    suspension: Suspension | None = None
    cancellation_requested: bool = False   # ← 取消意图字段（PG 持久化），不是状态
    version: int = 1                  # Optimistic Lock

class ExecutionStatus(str, Enum):
    PENDING   = "PENDING"
    RUNNING   = "RUNNING"
    STALE     = "STALE"               # Lease 过期 / Worker 失联（中间态，必进 Recovery）
    SUSPENDED = "SUSPENDED"
    COMPLETED = "COMPLETED"
    FAILED    = "FAILED"
    CANCELLED = "CANCELLED"

@dataclass(frozen=True)
class Suspension:
    reason: str                       # HUMAN_APPROVAL / CHILD_AGENT / TIMER / EXTERNAL_EVENT
    detail: dict[str, Any] = field(default_factory=dict)
    suspended_at: datetime = field(default_factory=now)
```

> ⚠️ **Kernel 里不存在 `TaskExecution` / `ToolExecution` 领域对象。**
> 领域实体只有 `Task / Execution / Attempt`。
> `execution_mode` 描述的是**执行模式**（无状态短任务 vs 长任务 Actor），不是独立实体类型。
> 一个 Tool Call 产生的是 `Task(task_type=TOOL)`，不是 ToolExecution。

**不变量**
- `status = SUSPENDED` 时 `suspension` 必填；非 SUSPENDED 时必须为 `None`
- **进入 SUSPENDED 必须释放 Lease**（否则 Worker 被长任务占死）
- 终态 `COMPLETED / FAILED / CANCELLED` 不可逆
- 禁止直接赋值 `status`，必须走 `StateMachine.transition()`
- 每次状态变更 `version += 1`，更新需带 version 校验
- **禁止出现 `TaskExecution` / `ToolExecution` 类**
- `idempotency_key` 全 Execution 唯一，且在同一 Execution 的所有 Attempt 间保持不变
- **`STALE` 只能由 `RUNNING` 进入**；STALE 必须经 Recovery，禁止直接回 `RUNNING`
- `cancellation_requested` 是**意图字段，不是第七个状态**；真正的 `CANCELLED` 由 Kernel 写入
- 任何写回必须携带 `lease.fencing_token`，落后即拒绝

---

### 3.3 Attempt（一次具体尝试）

```python
@dataclass
class Attempt:
    attempt_id: str
    execution_id: str
    attempt_no: int                   # 从 1 开始，单调递增
    status: AttemptStatus
    started_at: datetime | None = None
    finished_at: datetime | None = None
    error: ErrorInfo | None = None
    result: dict[str, Any] | None = None
    checkpoint_id: str | None = None
    version: int = 1

class AttemptStatus(str, Enum):
    PENDING  = "PENDING"
    RUNNING  = "RUNNING"
    SUCCEEDED= "SUCCEEDED"
    FAILED   = "FAILED"
    TIMEOUT  = "TIMEOUT"
    CANCELLED= "CANCELLED"
```

> **Retry ≠ 状态回退。** 失败即开新 Attempt，保留失败证据：
> `Attempt #1 → FAILED(Tool timeout)` / `Attempt #2 → SUCCEEDED(Fallback model)`

**不变量**
- 一个 Execution **同一时刻最多一个** `RUNNING` 的 Attempt
- `attempt_no` 单调递增，不可复用
- `TIMEOUT` 是 Attempt 层的失败原因 → 若重试耗尽，Execution 落到 `FAILED`
- 只有 `RUNNING` 的 Attempt 才能持有有效 Lease

---

### 3.4 Lease

```python
@dataclass
class Lease:
    lease_id: str
    execution_id: str             # ← Lease 挂 Execution，不挂 Task
    attempt_no: int
    worker_id: str
    fencing_token: int            # 单调递增，Claim 时分配
    acquired_at: datetime
    expires_at: datetime
    heartbeat_at: datetime
    version: int = 1

    def is_expired(self, now: datetime) -> bool:
        return now > self.expires_at

    def authorize(self, token: int) -> None:
        """写回校验：token 落后于当前值 → 拒绝（防僵尸 Worker 污染状态）。"""
        if token < self.fencing_token:
            raise StaleWriteError(
                f"rejected stale write: token={token} < current={self.fencing_token}"
            )

class StaleWriteError(Exception):
    """过期持有者的写回。"""
```

**不变量**
- Lease 过期后，原 Worker 不得再写该 Execution
- Recovery Controller 定期扫描过期 Lease → `Execution = STALE` → Recovery → 开新 Attempt
- **禁止 `RUNNING → QUEUED` 直接回退**
- `fencing_token` 单调递增，Claim 时分配；**所有写回必须经 `authorize()` 校验**
- 三者不是一回事：`Lease` = 谁拥有执行权（会过期）／`Lock` = 短临界区互斥／`Fencing Token` = 防过期持有者回写

---

### 3.5 Checkpoint

**Checkpoint 必须分两层**（否则 Kernel 会被业务语义污染 —— Kernel 不知道什么是 Step）：

```python
@dataclass(frozen=True)
class KernelCheckpoint:                   # Execution 级，Kernel 拥有
    checkpoint_id: str
    execution_id: str
    attempt_no: int
    seq: int                              # 同一 Attempt 内递增
    execution_state: dict[str, Any]
    idempotency_key: str
    fencing_token: int
    artifact_refs: list[ArtifactRef]
    recovery_metadata: dict[str, Any]
    created_at: datetime = field(default_factory=now)

@dataclass(frozen=True)
class RunCheckpoint:                      # Run 级，Runtime / Harness 拥有
    checkpoint_id: str
    run_id: str
    current_step: str
    completed_tasks: list[str]
    variables: dict[str, Any]
    context_snapshot_id: str | None       # ← 只存 ID 引用，不内嵌
    created_at: datetime = field(default_factory=now)
```

**写入时机**（不定义就会在实现时随机化）：

| 时机 | 写什么 |
| --- | --- |
| Attempt 成功 | `KernelCheckpoint` |
| Step 完成 | `RunCheckpoint` |
| 进入 SUSPENDED 前 | 两者都写，强制 |
| 每次 LLM 调用后 | ❌ 不写 Checkpoint，只写 ContextSnapshot |

> **Checkpoint ≠ ContextSnapshot**
> - `KernelCheckpoint` = **执行到哪里了 + 恢复需要什么** → 恢复用
> - `RunCheckpoint` = **跑到哪个 Step / 完成哪些 Task** → 断点续跑用
> - ContextSnapshot = **模型上下文是什么** → 复现 / 审计用（`RunCheckpoint` **引用**它，不内嵌）
>
> 若写成 `checkpoint = messages`，就退化成"大 Context 对象"了。

**不变量**
- `KernelCheckpoint` 归属具体 Attempt；`RunCheckpoint` 归属 Run
- 新 Attempt 可继承最近一个 `KernelCheckpoint`（用于断点续跑）
- `context_snapshot_id` 必须是引用，**不得内嵌上下文内容**
- 两者都不可变（frozen）

---

## 4 · 全局不变量清单（M15 验收标准）

### Intelligence 层

| # | 不变量 |
| --- | --- |
| I-1 | Goal 必须由 UserRequest 经 Goal Interpreter 生成，不可跳过 |
| I-2 | Goal 必须含至少一条 success_criteria |
| I-3 | State 只能通过 `apply(observation)` 变更 |
| I-4 | Decision 不可直接执行，必须经 ActionResolver → TaskFactory |
| I-5 | Decision / Observation / Checkpoint **不可变**（frozen，支撑 Replay） |
| I-6 | Observation 只能来自真实执行结果，不可凭空构造 |
| I-7 | 大文件必须走 Artifact → S3，Observation 只存引用 |
| I-8 | Action 为 `HUMAN_APPROVAL` 时必须带 timeout |
| I-9 | `confidence_signal` **不得**用作概率或自动执行阈值（Policy 决定，不由它决定） |

### Execution 层

| # | 不变量 |
| --- | --- |
| E-1 | 状态变更必须走 `StateMachine.transition()`，**禁止直接赋值** |
| E-2 | 终态（COMPLETED / FAILED / CANCELLED）不可逆 |
| E-3 | 禁止 `RUNNING → QUEUED` 直接回退 |
| E-4 | Retry = 开新 Attempt，不是状态回退 |
| E-5 | 同一 Execution 同时最多一个 RUNNING Attempt |
| E-6 | attempt_no 单调递增，不可复用 |
| E-7 | 只有 RUNNING Attempt 能持有有效 Lease |
| E-8 | SUSPENDED 必须携带 suspension_reason |
| E-9 | 进入 SUSPENDED 必须释放 Lease |
| E-10 | Task 必须带全局唯一 idempotency_key |
| E-11 | Task 必须能溯源到 step_id（**一个 Step 可对应多个 Task**） |
| E-12 | Scheduler 只认识 Task，不认识 Agent / Step |
| E-13 | 所有对象带 version，更新走 Optimistic Lock |
| E-14 | **禁止定义 `TaskExecution` / `ToolExecution` 类** —— Kernel 只有 Task/Execution/Attempt |
| E-15 | StateMachine 是通用机制；第一版实现 `ExecutionStateMachine` + `AttemptStateMachine` |
| E-16 | Wake-up（唤醒 SUSPENDED）必须由 **Kernel** 执行，产出 Runnable Task 交 Scheduler |
| E-17 | Cancellation 生命周期管理属 Kernel；Harness 只能发起请求 |
| E-18 | 进入 SUSPENDED 必须释放 Lease 并记录 wait condition |
| E-19 | **`Task : Execution = 1 : 1`** —— 一个 Task 最多一个 Execution，重跑必须新建 Task |
| E-20 | **Lease 挂在 Execution**，不挂在 Task；Scheduler 选 Task、Claim Execution |
| E-21 | `idempotency_key = execution_id`，在同一 Execution 的所有 Attempt 间保持不变 |
| E-22 | 所有写回必须携带 `fencing_token`，落后即抛 `StaleWriteError` |
| E-23 | `STALE` 只能由 `RUNNING` 进入，且必须经 Recovery，禁止直接回 `RUNNING` |
| E-24 | Kernel Checkpoint 不得包含 `current_step` / `completed_tasks`（业务语义属 Run Checkpoint） |

### 跨层

| # | 不变量 |
| --- | --- |
| X-1 | Runtime 产出 Task 即交棒，不再触碰生命周期 |
| X-2 | Kernel 不认识 Goal / Decision，只认识 Task |
| X-3 | 每次状态变更必须产生一个 Event（写入 Outbox，同事务） |
| X-4 | Event ≠ Observation：Event 给系统与审计，Observation 给 Agent |
| X-5 | PostgreSQL 是唯一 Truth；Kafka 是 Durable Event Log，非 Truth |
| X-6 | **AgentRun ≠ Kernel Execution**：AgentRun 是业务 Run，其内部 Task 由 Kernel 管理 |
| X-7 | Observation 必须经 StateReducer 才能更新 State，禁止直接写入 |
| X-8 | **Execution Recovery（Kernel，救回 Execution）≠ Agent Recovery（Harness，失败后怎么办）** |
| X-9 | **Step 属 Business Domain**，是 Plan Node 的运行实例；Kernel 只看到它派生的 Task |
| X-10 | 同一 run 的 State 写入必须串行化（version 校验 + 冲突重放），禁止并发覆盖 |
| X-11 | Harness 只能"请求"挂起 / 取消，**生命周期归 Kernel**；Harness 不反向调用 Kernel |
| X-12 | Gateway Fallback 属单次 Attempt 内部容错，**不产生新 Attempt**（防重试放大） |

---

## 5 · 合法状态转换表

对齐代码 `packages/agent_domain/execution/state_machine.py::EXECUTION_TRANSITIONS`。

| From | To | 触发者 | 备注 |
| --- | --- | --- | --- |
| `PENDING` | `RUNNING` | Worker（claim） | Atomic Claim + Lease + fencing_token |
| `PENDING` | `CANCELLED` | Kernel | 未开跑即取消 |
| `RUNNING` | `PENDING` | Kernel | 可重试失败 → 回到可调度，等 Scheduler 重新 Claim |
| `RUNNING` | `SUSPENDED` | Kernel（Harness 请求） | 必须带 reason + wait condition，**必须释放 Lease** |
| `RUNNING` | `STALE` | Recovery Controller | Lease 过期 / Worker 失联 |
| `RUNNING` | `COMPLETED` | Worker | Attempt 成功 |
| `RUNNING` | `FAILED` | Kernel | 重试耗尽 / 不可重试 |
| `RUNNING` | `CANCELLED` | Kernel | Cancellation Token |
| `STALE` | `FAILED` / `CANCELLED` | Recovery / Policy | **STALE → RUNNING 不在表中** |
| `SUSPENDED` | `PENDING` | **Kernel Wake-up** | 重新变成 Runnable Task，再被 Claim |
| `SUSPENDED` | `FAILED` / `CANCELLED` | Policy | 挂起中被取消 / 终止 |

Attempt 级：`RUNNING → SUCCEEDED / FAILED / TIMEOUT / CANCELLED`（终态对象，不做二次转换）

> **为什么没有 `STALE → RUNNING`**：Lease 过期后必须经 `ExecutionAggregate.recover()`
> 开**新 Attempt + 新 fencing_token**，不允许直接改状态续跑（E-23）。
> 这就是"Lease 过期不是简单把状态改成 QUEUED"的代码化表达。
>
> **为什么 `SUSPENDED → PENDING` 而不是直接 `→ RUNNING`**：Wake-up 的产物是
> **Runnable Task**，最终执行权仍由 Scheduler / Worker 的 Claim 决定，Kernel 不代劳。

### Wake-up 机制（SUSPENDED 闭环的关键）

只写 `RUNNING ⇄ SUSPENDED` 是不够的 —— 必须回答**谁负责唤醒**：

```
Suspended Execution
        ↓
Wait Condition   (HUMAN_APPROVAL / CHILD_AGENT / TIMER / EXTERNAL_EVENT)
        ↓
Event / Timer 到达
        ↓
Wake-up                    ← Kernel 的 Resume 机制
        ↓
Runnable Task              ← 重新变成可调度单元
        ↓
Scheduler
        ↓
继续执行（用 Checkpoint 恢复）
```

> Wake-up 属 **Kernel**，不是 Harness。否则 Actor + Mailbox、Event 唤醒 Resume 都没真正落到执行模型上。

### Cancellation 归属

```
User / Policy / System
        ↓
Cancellation Request          ← Harness 可发起
        ↓
Kernel Cancellation           ← 生命周期管理属 Kernel
        ↓
Worker CancellationToken
        ↓
Execution terminated
```

### StateMachine 是通用机制

```
StateMachine（通用）
├── AgentRunStateMachine
├── TaskStateMachine
├── ExecutionStateMachine     ← 第一版实现
├── AttemptStateMachine       ← 第一版实现
└── WorkflowRunStateMachine   （未来）
```

**第一版只需实现 `ExecutionStateMachine` + `AttemptStateMachine`**，AgentRun 状态由它们派生。

---

## 6 · 目录结构（阶段 1）

```
packages/agent_domain/
├── __init__.py
├── ids.py                 # ID 生成器（run_/task_/exec_/attempt_ 前缀）
├── errors.py              # 领域异常（InvariantViolation / InvalidTransition）
│
├── intelligence/
│   ├── goal.py
│   ├── state.py
│   ├── decision.py
│   ├── action.py
│   ├── observation.py
│   └── plan.py
│
├── execution/
│   ├── task.py
│   ├── execution.py
│   ├── attempt.py
│   ├── lease.py
│   ├── checkpoint.py
│   └── state_machine.py   # transition() 唯一入口 + 转换表
│
└── events/
    └── event.py           # Event 定义（与 Observation 严格区分）
```

**阶段 1 的 Definition of Done**：
纯 Python 实现 + 单元测试覆盖全部 **36 条不变量**（I-1~I-9、E-1~E-24、X-1~X-12），
**不接数据库、不接 Kafka、不接 FastAPI**。

### 阶段 1 实现状态：已完成

```text
packages/agent_domain/            纯 Python，零基础设施依赖
├── ids.py / errors.py
├── intelligence/                 goal / state / decision / action / observation / plan
├── execution/                    task / execution / attempt / lease / checkpoint
│                                 retry（FailureClass + RetryPolicy）
│                                 state_machine（Execution + Attempt）
│                                 aggregate（Kernel 侧强一致边界）
└── events/                       Event（与 Observation 严格区分）

tests/unit/                       （见文末：阶段 5 完成后共 109 个单测）
```

运行：

```bash
python -m unittest discover -s tests/unit -t .
```

不变量的代码化方式（不是写在文档里，而是写在对象内部）：

| 机制 | 落地位置 |
| --- | --- |
| 状态禁止直接赋值 | `Execution.__setattr__` 拦截受保护字段，只允许 `mutating()` 窗口写入 |
| State 只能经 Reducer | `State.__setattr__` 拦截，唯一入口 `apply(obs, reducer, expected_version)` |
| 非法转换 | `StateMachine.assert_legal()` 查转换表 |
| 版本冲突（OCC） | `Execution.check_version()` / `State.apply(expected_version=)` |
| 僵尸 Worker 回写 | `Lease.authorize(token)` 抛 `StaleWriteError` |
| 每次状态变更有 Event | `StateMachine.transition()` 返回 `Event`（X-3） |

> 阶段 1 **没有** Scheduler / Repository / PG / Redis / Kafka。
> `ExecutionAggregate` 是内存实现，只用来固化语义；阶段 3 会在同样的接口上换成真实 Kernel。

### 实施顺序（严格遵守"先执行模型，再基础设施"）

```
阶段 1  Domain Model          ✅ 已完成  agent_domain（1959 行）
阶段 2  State Machine         ✅ 已完成  Execution + Attempt 转换表（E-1/E-2/E-23 可测）
阶段 3  Execution Kernel      ✅ 已完成  Scheduler / Recovery / Wake-up / Cancellation /
                                        Idempotency（In-Memory 端口实现）
阶段 4  Repository Interfaces ✅ 已完成  ports.py：Repository / Outbox / CancelSignal /
                                        Idempotency，不绑定任何数据库
阶段 5  PostgreSQL            ✅ 已完成  表结构由上面的生命周期**反推**出来 + Outbox
阶段 6  Redis                 ✅ 已完成   Lease 索引 / 取消信号 / 幂等缓存（全部可丢、可重建）
阶段 7  Kafka + Worker        ✅ 已完成   Outbox Publisher → Event 驱动 + Worker 闭环
阶段 8  最小 Agent Loop        ✅ 已完成   一个 LLM + 一个 Tool；全栈闭环验收通过
```

> **M15 八阶段全部完成。** 172 个测试全绿，闭环见 `tests/unit/test_full_stack.py`。

**阶段 3 / 4 产物**（`packages/execution_kernel/`）：

```text
kernel.py               ExecutionKernel：submit / claim / heartbeat / complete / fail
                        / suspend / wakeup / recover / request_cancel / cancel
scheduler.py            Scheduler：priority + tenant fairness + capability 匹配 → Claim
recovery_controller.py  Lease 过期 → STALE → 新 Attempt（E-23 的唯一入口）
wakeup_controller.py    Wait Condition 满足 → PENDING（Runnable Task）
cancellation.py         三段式：Durable Intent / Fast Signal / CancellationToken
idempotency.py          同 key 只执行一次；EXTERNAL_UNKNOWN 必须回查不盲重试
ports.py                抽象接口（阶段 4，不 import 任何 DB 客户端）
inmemory.py             端口的内存实现（阶段 5/6 换成 PG / Redis / Kafka）
```

测试：`tests/unit/test_kernel.py`（15 个用例），随阶段 1 一起跑。

> 关键约束：**Kernel 不认识 Goal / Decision，也不认识数据库**。
> 时间由 `Clock` 端口注入（领域层不自己取系统时间），否则 Lease 过期判断无法测试。

> `Domain → PostgreSQL → Kernel` 是错的：PG Schema 应由 Domain + Kernel 的生命周期**定义出来**，
> 而不是反过来决定 Domain。

### 阶段 5 实现状态：已完成（PostgreSQL）

```text
infrastructure/postgres/001_kernel.sql
├── tasks               E-11 step_id 可溯源
├── executions          Lease 内联（1:1，必须与状态同事务）+ version 乐观锁
│                       uq_executions_task（E-19）/ uq_executions_idem（E-21）
│                       ck_executions_status / ck_executions_suspension（E-8）
│                       idx_exec_status / idx_exec_lease_expiry（部分索引）
├── attempts            uq_attempts_no（E-6：attempt_no 单调递增不可复用）
├── kernel_checkpoints  执行态：execution_state / idempotency_key / fencing_token / artifact_refs
├── run_checkpoints     业务态：current_step / completed_tasks / variables（E-24 分层）
└── outbox_events       X-3：状态与事件同事务；idx_outbox_pending 部分索引

packages/execution_kernel/adapters/postgres.py
├── PostgresExecutionRepository   save() 带 WHERE version = %s，rowcount = 0 → ConcurrentStateError（E-13）
├── PostgresAttemptRepository     (execution_id, attempt_no) 幂等 upsert
└── PostgresOutboxStore           append / pending / mark_published
```

**新增端口 `AttemptRepository`（阶段 5 的关键修正）**：

原先 `ExecutionKernel.aggregate()` 从 Repository 重建 Execution 时**不会**恢复 Attempt 历史，
导致进程重启后 Kernel 以为自己是"第 1 次尝试" —— E-4（Retry = 新 Attempt）与 Retry 预算
在重启场景下直接失守。PG 里明明有 `attempts` 表却没有端口把它接回来。

修法：

```python
# ports.py
class AttemptRepository(Protocol):
    def save(self, attempt: Attempt) -> None: ...          # 幂等 upsert
    def get(self, execution_id: str, attempt_no: int) -> Attempt | None: ...
    def list_by_execution(self, execution_id: str) -> Sequence[Attempt]: ...

# kernel.py
def aggregate(self, execution_id):
    ...
    agg = ExecutionAggregate(execution)
    if self.attempts is not None:
        agg.attempts = list(self.attempts.list_by_execution(execution_id))  # 必须读回来

def _persist(self, agg, ...):
    self.repository.save(agg.execution, expected_version=...)
    self._persist_attempts(agg)      # 整列表 upsert，幂等所以安全
```

**不变量在数据库层的兜底**（Domain 拒绝是"善意"，DB 约束才是"兜底"，两者都要有）：

| 不变量 | DB 约束 | 测试 |
| --- | --- | --- |
| E-6  attempt_no 不可复用 | `UNIQUE(execution_id, attempt_no)` | `test_e6_attempt_no_unique_per_execution` |
| E-8  SUSPENDED ⇔ suspension_reason | `CHECK` 双向 | `test_e8_*`（4 个用例） |
| E-13 乐观锁 | `UPDATE ... WHERE version = %s` + rowcount | `test_stale_save_raises_concurrent_state_error` |
| E-19 Task : Execution = 1 : 1 | `UNIQUE(task_id)` | `test_e19_one_execution_per_task` |
| E-21 幂等键稳定且唯一 | `UNIQUE(idempotency_key)` | `test_e21_idempotency_key_unique` |
| X-3  事件与状态同事务 | `outbox_events` | `test_events_land_in_outbox` |

**测试策略**：`tests/unit/sqlite_shim.py` 是一个只做三件事的方言替身
（`%s`→`?`、`ANY(%s)`→`IN (...)`、`TIMESTAMPTZ`→`datetime`），
但 DDL 直接吃 `001_kernel.sql` 的**原文** —— 所以测的确实是真 schema，
而不是另写一份 sqlite 版自欺欺人。真实环境换成 psycopg 即可，业务代码一行不动。

```bash
python -m unittest discover -s tests/unit -t .
# Ran 109 tests ... OK
```

### 阶段 6 实现状态：已完成（Redis）

```text
packages/execution_kernel/adapters/redis.py
├── RedisLeaseIndex         ZSET(member=execution_id, score=expires_at) → due(now)
├── RedisCancelSignalStore  SET cancel:{id} 1 EX ttl（通知，不是裁决）
├── RedisIdempotencyStore   SET idem:{key} <json> NX EX ttl（首次写入者胜）
└── rebuild_lease_index()   从 PG 的 executions 重建索引 —— Redis 全丢的证据
```

**新增端口 `LeaseIndex`**（`track` / `forget` / `due` / `clear`）：
Recovery Controller 不必每轮全表扫 PG。但它只回答"谁的 Lease 快到期了"，
**不回答"谁持有 Lease"** —— 后者只有 PG 说了算，所以拿到 due 列表后仍要回 PG 复核。

索引是 PG 的**派生视图**，方向只能是 PG → Redis：

```python
# kernel._persist()
def _sync_lease_index(self, agg):
    lease = agg.execution.lease
    if lease is not None and not agg.execution.is_terminal:
        self.lease_index.track(lease)      # ZADD 按 member 覆盖，天然幂等
    else:
        self.lease_index.forget(agg.execution.execution_id)
```

#### 关键设计决策：Redis 丢了怎么办

写测试时发现一个必须回答的问题：Recovery 只走索引的话，Redis 一挂就**永远扫不到**。
有人会想"索引返回空就回落到 PG" —— 这是错的：**正常空闲时索引本来就是空的**，
那样等于每轮都全表扫，索引白建了。

正确做法是**低频兜底**，把两种失败模式分开定价：

```python
@dataclass
class RecoveryController:
    sweep_every: int = 10     # 每 N 个周期额外做一次 PG 全量兜底扫

    def scan(self):  ...      # 快路径：Redis 索引
    def sweep(self): ...      # 安全网：直接扫 PG（慢，但不依赖 Redis）
    def run_once(self):
        self.ticks += 1
        stale = self.scan()
        if self.ticks % self.sweep_every == 0:
            stale += self.sweep()          # 兜底
        return {"stale": stale, "recovered": self.recover()}
```

> Redis 是快路径，PG 是安全网。**Redis 全丢只允许造成"变慢"（STALE 晚 N 个周期被发现），
> 不允许造成"变错"（永远漏掉）。**

#### 三条硬规则（写在模块文档里，也由测试证明）

| 规则 | 含义 | 用例 |
| --- | --- | --- |
| 只做加速，不做裁决 | 取消的事实来源是 PG 的 `cancellation_requested` | `test_cancellation_survives_signal_loss`：flushall 后 `is_requested()` 仍为 True |
| 丢失可重建 | `rebuild_lease_index()` 从 PG 恢复 | `test_index_is_rebuildable_after_total_loss` |
| 读不到 = UNKNOWN | `IdempotencyStore.get()` 返回 None 必须回查，不能盲重试 | `test_missing_means_unknown_not_never` |

```bash
python -m unittest discover -s tests/unit -t .
# Ran 129 tests ... OK   （新增 20 个 Redis 用例）
```

> `tests/unit/fake_redis.py` 只实现了用到的 7 条命令（SET NX/EX、GET、DEL、ZADD、
> ZRANGEBYSCORE、ZREM、FLUSHALL），TTL 惰性判定且时间可注入 ——
> 这样"过期"和"Redis 整个没了"都能确定性地测。

### 阶段 7 实现状态：已完成（Kafka + Worker）

```text
publisher.py          OutboxPublisher：pending → publish → mark_published
adapters/kafka.py     KafkaEventPublisher（topic 分流 / key = aggregate_id / 批次末尾 flush）
                      encode_event / decode_event（event_id 在**消息体**里）
consumers.py          IdempotentConsumer + ProcessedEventStore 端口
worker.py             Worker：dispatch → execute → complete/fail（fencing + 心跳 + 协作取消）
infrastructure/postgres/002_outbox_consumer.sql   processed_events 去重表
```

#### 投递语义：至少一次，所以去重是正确性的一部分

顺序必须是 **先 publish，再 mark_published**：

```
publish() 成功 → 进程崩溃 → 没来得及 mark → 下一轮 drain 重投
```

反过来（先 mark 后 publish）会**丢事件** —— 丢事件意味着 Read Model / 审计永久缺一块且无从发现，
比重复严重得多。所以选重复，然后把去重做对。

测试直接复现了这条链路（`test_redelivery_is_deduplicated_by_consumer`）：
构造一个"投递成功但随后崩溃"的 publisher，Kafka 侧收到 2 条，消费者只处理 1 次。

Kafka 侧两个必须做对的点：

| 点 | 为什么 |
| --- | --- |
| `key = aggregate_type:aggregate_id` | 同聚合同分区 → 分区内保序；否则消费者会先看到 `completed` 再看到 `running` |
| `event_id` 放进**消息体** | header 会在跨系统转发 / 重放时丢，去重键必须跟着 payload 走 |
| 批次末尾只 flush 一次 | 批次内多次 flush 会破坏顺序 |

去重记录落在 **PG 而不是 Redis**（`002_outbox_consumer.sql`）：
Redis 丢了会导致整批历史事件被重跑一遍。去重属于"正确性"，不属于"快路径" ——
这与阶段 6 的 Lease 索引正好相反，两者的差别就是判据。

#### Worker：五件必须做对的事

1. **回写带 fencing_token**（E-22）—— 僵尸 Worker 的写必须被拒
2. **取消是协作式的** —— Token 只在 Worker 主动检查时生效；换来的是"不会在任意指令处被撕裂"
3. **心跳续租** —— `heartbeat_interval` 必须 `< lease_ttl`（构造时校验）
4. **失败要分类** —— 异常翻译成 `FailureClass`；`EXTERNAL_UNKNOWN` 不重试（要回查）
5. **丢 Lease 就停手** —— `StaleWriteError` / `LeaseRequired` 一律转 `LOST_LEASE`，绝不写结果

#### 阶段 7 期间修掉的三个真实缺口

写 Worker 测试时发现的，都不是测试写错：

**① 取消意图没人推进到终态。**
`request_cancel()` 只写 PG 的 `cancellation_requested`。若 Execution 还是 PENDING（没开工），
就没人会去响应这个意图 —— 它会永远悬在那里（而且 Scheduler 还会因为 E-17 拒绝 claim）。

修法是补 `CancellationService.sweep()`，规则很朴素：

> **只有真正持有有效 Lease 的 RUNNING 才需要等 Worker 协作；其余一律直接判死。**

```
PENDING / SUSPENDED / STALE / RUNNING+Lease已过期  → 立刻 CANCELLED
RUNNING + Lease 有效                                → 继续等（Worker 在安全点响应）
```

**② Scheduler 会把"已请求取消"的任务派发出去。**
明知道马上要取消还开工，白占一个 Attempt，还会撞上 E-17 把 Worker 打挂。
修法：`Scheduler.candidates()` 过滤 `cancellation_requested`。

**③ Worker 在 Lease 过期后会崩。**
`complete()` 抛的是 `LeaseRequired`（不是 `StaleWriteError`），Worker 没接住。
修法：两者都转成 `LOST_LEASE` —— 结果不能写进去，交给接管方（Recovery 会开新 Attempt）。

```bash
python -m unittest discover -s tests/unit -t .
# Ran 157 tests ... OK   （新增 28 个：publisher 11 + worker 17）
```

### 阶段 8 实现状态：已完成（最小 Agent Loop）

```text
packages/agent_runtime/
├── ports.py         Planner / DecisionEngine / LLMClient / Tool
├── loop.py          AgentLoop：Plan → Decision → Action → Task → Kernel → Observation → State
├── reducer.py       RuntimeReducer：Observation → State 的唯一解释器（X-7）
├── task_factory.py  Action → Task 的边界（I-4 / X-1）
└── executors.py     一个 LLM（HTTP）+ 一个 Tool（NATIVE）
```

Runtime 只**驱动**，Loop 里没有任何生命周期状态机，也没碰过数据库 ——
那些全在 Kernel。Loop 能看见的 Kernel 接口只有三个：`submit` / `worker.run_once` / 读 Execution。

#### 循环长这样

```
start(user_request) ──► interpret_goal ──► Goal ──► State
        │
        ▼
 step(): current_plan 为空？ → planner.plan() → 以 Observation(plan.created) 进 State
        │
        ▼
    decision_engine.decide(state) → Decision
        │
        ├─ ActionType.FINISH      → 结束
        ├─ ActionType.REPLAN      → Observation(plan.invalidated)，下轮重新规划
        ├─ 高风险 / HUMAN_APPROVAL → 挂起等 Harness（X-11），不执行
        └─ 其余                    → TaskFactory → Task → kernel.submit（X-1 交棒）
                                        │
                                   Worker/Scheduler 执行
                                        │
                                   Observation.from_execution_result（I-6）
                                        │
                                   state.apply(obs, reducer, expected_version)（X-10）
```

#### 阶段 8 期间修掉的两个真缺口

**① Reducer 其实写不了 State 的不可变字段。**
`State.__setattr__` 只放行 `_sealed` / `version`，于是 Reducer 只能偷偷改 list / dict
（可变容器绕过了 `__setattr__`），`current_plan` 这种字段根本没法合法写入 ——
**I-3 等于只对一半字段生效**。

修法仿照 `Execution.mutating()`，给 State 一个只由 `apply()` 打开的写入窗口：

```python
@contextmanager
def reducing(self):
    ...   # 只在这里允许写；业务代码依然不能直写 State

def apply(self, obs, reducer, expected_version=None):
    self.observations.append(obs)        # 事实：append-only
    with self.reducing():
        reducer.reduce(self, obs)        # 解释
    self.version += 1
```

**② `ActionType` 缺 `LLM_CALL`。**
基线里 `Task(type=LLM_CALL)` 是明确存在的，但 Intelligence 侧的 `ActionType` 没有对应值 ——
"调模型"和"调工具"是两种不同的决策，混成 `TOOL_CALL` 会让 Action 语义失真。
补上并映射为 `(TaskType.LLM_CALL, ExecutorType.HTTP)`（LLM 走 HTTP，与本地工具用不同 Executor）。

另外确认了一条容易写错的语义：**"没有下一步"不是用空 Decision 表达，而是用 `ActionType.FINISH`**。
`Decision.selected_action` 构造时就必填，留空等于给 I-4 开一个"副作用来源不明"的口子。

#### 失败也是事实

执行失败同样会变成 Observation 进 State（`kind=execution_failed`），
`runtime_status` 不会变成 FINISHED —— 因为**失败也是事实**，Agent 要靠它决定
下一步是 Replan、换 Tool 还是放弃。这是 Loop 真的能"循环"起来的前提。

### 阶段 8 验收：全栈闭环

`tests/unit/test_full_stack.py` 把阶段 1~8 一次性接起来（PG + Redis + Kafka + Worker + Loop）：

| 断言 | 对应 |
| --- | --- |
| Goal 达成、State 落到 FINISHED | Runtime 闭环 |
| 两个 Execution 都在 PG 里 COMPLETED | Kernel + PG 适配器 |
| Attempt 历史可回读 | 阶段 5 补的 `AttemptRepository` |
| 终态 Execution 不在 Lease 索引里 | 阶段 6 |
| Outbox → Kafka → 重复投递只处理一次 | 阶段 7 |
| `flushall()` 后结果不变、索引可从 PG 重建 | "Redis 只是快路径" |

```bash
python -m unittest discover -s tests/unit -t .
# Ran 172 tests ... OK
```

**代码量**：`packages/` 4810 行 + `tests/` 3670 行，零第三方依赖（PG/Redis/Kafka 客户端都是 duck-typed）。

---

## 阶段 9：M16 Harness（Policy / Guardrail / Cost / HITL）— 已完成

阶段 8 的 Loop 里，`_needs_approval` 只是一句本地判断 —— **Harness 根本不存在**，
基线 §2 钉死的那个"唯一拦截点"在代码里没有落点。本阶段把它补上。

新增 `packages/agent_harness/`（6 文件）+ `tests/unit/test_harness.py`（40 用例）。
总测试 **212 个，全绿**。

```
packages/agent_harness/
├── ports.py       Clock（自己定义，不复用 Kernel 的 —— 见下）
├── policy.py      PolicyEngine / PolicyRule / PolicyDecision / Verdict
├── guardrail.py   GuardrailEngine / 四阶段 / 三档严重程度
├── cost.py        CostManager / Budget
├── approval.py    HumanLoop / ApprovalRequest / ApprovalStore
└── harness.py     Harness（Runtime 唯一能调的入口）
```

### 新增不变量 H-1 ~ H-8

| # | 内容 |
| --- | --- |
| H-1 | DENY / REQUIRE_APPROVAL 必须带 reason（审计要知道为什么） |
| H-2 | REQUIRE_APPROVAL 必须带那条 `ApprovalRequest`（否则 Loop 不知道审批谁） |
| H-3 | `confidence_signal` 传不进 Policy —— `PolicyContext` 里**没有**这个字段 |
| H-4 | Harness 字段里没有 kernel（断言 `Harness.__dataclass_fields__`） |
| H-5 | 审批必须有 `expires_at` —— 没有它就是允许一个人把 Run 永久挂住 |
| H-6 | 终态审批不可再改（否则"先批后驳"会让审计自相矛盾） |
| H-7 | Guardrail BLOCK 不给人审批绕过的机会；BLOCK 在场就不再谈 REVIEW |
| H-8 | 过期审批不能被批准 —— 必须先判定 EXPIRED，让 Run 走超时分支 |

### H-3：把 I-9 从约定变成类型约束

> `PolicyContext` 里**根本没有** `confidence` 字段。
> 不是"建议你别用"，是"你传不进来"。

测试直接断言字段名不存在，并且 `PolicyContext(confidence_signal=0.99)` 会 `TypeError`。
模型自评 0.99 也换不到一次自动放行，因为这条路在类型上就不存在。

### H-4：Harness 不持有 Kernel

`agent_harness` **不 import `execution_kernel`**（连 `Clock` 都自己定义了一份 Protocol）。
想挂起一个 Action 时，Harness 只交出 `ApprovalRequest`，
真正去 `kernel.suspend()` 的是 AgentLoop —— 谁持有 Kernel 引用谁动手。

### 撞到的三个真缺口

**1）`PENDING → SUSPENDED` 是非法转换**

`suspend()` 只允许从 `RUNNING` 转。一开始直接 `submit → suspend` 全红。
这不是实现的别扭，而是 `SUSPENDED` 的语义：**"执行权授出去过又被主动挂起"**，
"还没开始就挂起"那叫没调度。所以审批闸门必须 `submit → claim → suspend`。

副作用是闸门 Execution 的 Attempt 序列天然可解释：
**Attempt #1 = 发出询问（suspend 时 CANCELLED），Attempt #2 = 人的答复**。

**2）审批截止时间有两个旋钮**

`AgentLoopConfig.approval_timeout` 与 `HumanLoop.default_ttl` 各管一个，
会出现"审批还没过期、闸门 Execution 已经超时"的自相矛盾，且无人能发现。
改为**唯一事实源 = `ApprovalRequest.expires_at`**，闸门 Action 的 I-8 timeout 从它推出。

**3）过期审批不会自己变成终态**

与取消的 `sweep()` 同源：停在 `PENDING` 的审批没人扫就**永远挂着且不报错**。
补 `HumanLoop.expire_due()`。

### 接线后的 Loop 语义

```text
Decision → Action
     ↓
Harness.before_action(action)        ← 唯一拦截点（Runtime 主动调用）
     ↓
DENY              → 不产生 Task，Observation(policy.denied) 进 State
REQUIRE_APPROVAL  → 造闸门 Task → Kernel 写 SUSPENDED(HUMAN_APPROVAL)
ALLOW             → TaskFactory → Task → Kernel
```

> DENY 与 REQUIRE_APPROVAL 的关键区别：
> DENY 的 Action 永远不会执行，Kernel 里不该有它的痕迹；
> 但它**必须进 State** —— 否则 Agent 不知道自己被拒了，会一直重试同一个动作。
> 驳回同理（REJECTED 也是事实，不留 Observation 就会形成驳回—重试死循环）。

审批相关四类 Observation（`policy.denied` / `approval.requested` / `granted` / `rejected`）
**不进 `completed_tasks`** —— 审批不是业务 Task，是 Kernel 的一道闸门。

### 验收

```bash
python -m unittest discover -s tests/unit -t .
# Ran 212 tests ... OK
```

**代码量**：`packages/` 6080 行 + `tests/` 4260 行，仍然零第三方依赖。

---

---

## 阶段 10：Business Domain（AgentRun / Step / Run Checkpoint）— 已完成

### 为什么要先补它，而不是直接做阶段 9~11（Model Gateway / Tool Runtime）

规划里 `AgentRun` 和 `Step` 一直"存在"——§3.1 把它们列为 Business Domain，§4 把
`AgentRun : Step = 1 : N` 定成 P0。但阶段 1~8 写完时，代码里**只有 `run_id: str`**：

```python
@dataclass
class AgentLoop:
    state: State | None = None      # ← 有
    kernel: ExecutionKernel         # ← 有
    # AgentRun / Step：无
```

后果不是"少两个类"，而是**运行时没有地方能回答"这个 Run 现在怎么样了"**。
Model Gateway 和 Tool Runtime 接进来时，各自都得先回答这个问题，然后各自定义一遍，然后互相打架。
所以这一轮先把 Business Domain 补上。

### 新增文件

```
packages/agent_domain/business/
├── __init__.py     导出 AgentRun / Step / derive_* / AgentRunStateMachine
├── run.py          AgentRun + AgentRunStatus（B-1 ~ B-4）
├── step.py         Step + StepStatus（B-5 / B-6）
└── derive.py       derive_step_status / derive_run_status / AgentRunStateMachine（B-7）

packages/agent_runtime/checkpoints.py     RunCheckpointStore（§14）
tests/unit/test_business.py               36 个用例
```

### 投影链（不是状态机）

```text
Execution.status ──► Step.status ──► AgentRun.status
  （Kernel）          （Business）      （Business）
```

`AgentRunStateMachine` **不是转换器**：它只有 `can_transition()`，没有 `transition()`。
转换全是投影出来的，它只回答"这个投影结果合不合法"——
投影出一个非法跳转（比如 `COMPLETED → RUNNING`）说明下层语义出了问题，必须炸掉而不是默默接受。

这也顺手解决了 §9.2 与 §10 的表面冲突：

> §9.2 说"AgentRun 状态由下层派生"，§10 又给了它一套生命周期。
> 合起来的意思是：**它有状态集合与合法转换，但值不是被设置的，是算出来的。**
> 于是"Run 说 COMPLETED 了但还有 Task 在跑"这种不一致在结构上不可能发生——
> 因为根本没有第二个地方能记这个状态。

### B-1 ~ B-7

| # | 内容 | 落地方式 |
|---|---|---|
| B-1 | `AgentRun` 必须带 `goal` + `agent_id` | 构造期校验（I-2 在 Run 侧的对应物） |
| B-2 | `status` / `suspension_reason` 是派生值，不可直接赋值 | `__setattr__` 守卫 + `deriving()` 写窗口（与 `State.reducing()` 同构） |
| B-3 | Run 终态不可变 | `sync()` 内校验 |
| B-4 | `SUSPENDED` 必须带 `suspension_reason`；离开时自动清空 | `sync()` 内维护（与 E-8 同构） |
| B-5 | `Step.status` 由所属 Task 派生；`plan_node_id` 必填 | `__setattr__` 守卫 + 构造期校验 |
| B-6 | `Step : Task = 1 : N` | `Step.add_task()`（幂等）+ 调用方显式传 `step_id` |
| B-7 | Run 的终态只能由 Runtime 声明，**不能从 Step 派生** | `derive_run_status(runtime_terminal=...)` |

### 三个真问题（都是被测试逼出来的，不是想出来的）

**① B-7：Step 全绿 ≠ Run 完成**

第一版 `derive_run_status` 会从 Step 派生 `COMPLETED` / `FAILED`，
结果 `test_budget_exhausted_stops_the_loop` 直接崩：

```
B-3: run run_xxx is already completed; cannot become failed
```

崩得对。因为"所有 Step 都 COMPLETED"根本不代表"Run 完成了"：

- Agent 下一步可能还要 REPLAN（Step 全绿但目标没达成）
- Agent 可能**正在思考**——思考不产生 Task，在派生里是看不见的
- 一个 Step 永久失败后，Runtime 可以选择重规划而不是放弃

所以改成：**派生只产出活跃态**（`CREATED` / `QUEUED` / `RUNNING` / `SUSPENDED`），
三个终态全部由 Runtime 通过 `runtime_terminal=` 声明。
"所有 Step 已终态但 Runtime 没说话"→ `RUNNING`（它确实还活着，Loop 正握着它）。

> 一句话：**"手上的活干完了" ≠ "这个 Run 结束了"**。
> 前者是 Kernel 的事实，后者是 Runtime（乃至人）的判断。
> 把判断降格成聚合，等于让 Kernel 替 Runtime 决定什么时候收工。

**② §10 漏了 `CREATED → SUSPENDED` 这条边**

Run 创建后第一个 Action 就被判 `REQUIRE_APPROVAL` → 第一个 Step 直接 SUSPENDED →
Run 从未 RUNNING 过 → 状态机抛 `illegal AgentRun transition: created → suspended`。

不能靠"在 claim 之后、suspend 之前多采样一次 RUNNING"绕开——
**投影必须是顺序无关的纯函数**，从 Checkpoint 恢复时采不到中间那一帧，
同一份下层状态会投影出两种结果。所以补这条边。

**③ 字段名 `run` 把方法 `run()` 盖掉了**

```python
self.run = AgentRun(...)     # 从此 AgentLoop.run() 变成 'AgentRun' object is not callable
```

改名 `agent_run`。命名冲突本身就是边界不清的症状——
"跑到终态"和"这个 Run 对象"是两件事，不该共用一个名字。

### 接进 Loop 的改动

| 位置 | 改动 |
|---|---|
| `start()` | 创建真正的 `AgentRun`（之前只造 `State`） |
| `_ensure_step()` | 取当前 Step，没有就开一个；Step 是 **Plan Node 的运行实例**，在这里被"造出来"而不是从表里读出来 |
| `_execute()` | 显式传 `step_id=step.step_id`（B-6，不传就退化成 1:1） |
| `_suspend_for_approval()` | 闸门挂在当前 Step 上；挂起前**强制**写 Run Checkpoint（§14） |
| `_sync_after_execution()` | 每次执行后重投影：先 Step 后 Run（Run 的输入就是 Step 的状态） |
| `_finish()` | `_sync_run(terminal=COMPLETED)` —— Goal 达成是 Runtime 的判断，下层没有任何记录会说"Run 完成了" |
| 预算耗尽 | `_sync_run(terminal=FAILED)` |

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 248 tests ... OK   （新增 36 个：Business Domain）
```

**代码量**：`packages/` 6829 行 + `tests/` 4725 行，仍然零第三方依赖。

---

---

## M15 阶段 9：Model Gateway（§43 实施顺序）— 已完成

> 命名澄清：本节用的是 **§43 的 M15 实施顺序编号**（阶段 9 = Model Gateway）。
> 上面的"阶段 9：M16 Harness"和"阶段 10：Business Domain"是**实现轮次**编号，两者不是一套。

### 新增文件

```
packages/agent_runtime/model_gateway/
├── __init__.py     G-1 ~ G-7 清单
├── models.py       Model / Deployment / CompletionRequest / CompletionResponse
├── providers.py    ProviderAdapter / ProviderError / FunctionProvider
├── router.py       ModelRouter（返回候选链，不是单个）/ Rejection
└── gateway.py      ModelGateway（Fallback 链 + 轨迹）

tests/unit/test_model_gateway.py    28 个用例
```

### 最重要的一条：G-1

> **Gateway 内部的 Fallback 不产生新的 Kernel Attempt。**

```text
Attempt #1
   ↓
Provider A 500
   ↓
Gateway 内部 Fallback → Provider B      ← 仍然是 Attempt #1
   ↓
整条链都失败 → GatewayError              ← 只有这时 Kernel 才建 Attempt #2
```

§28 给的算式是 `Gateway Fallback(3) × Kernel Retry(3) = 9 次调用`。
更准确地说，真正要防的是 **Attempt 数爆炸**而不是单纯的调用次数：
如果 Gateway 每次内部失败都算一个 Attempt，就变成
`3 次 fallback → 3 个 Attempt → 每个 Attempt 又重试 3 次`，是指数级的。

所以 Gateway 必须**整体对外呈现为一次调用**：
成功给一个 `GatewayCall`（里面带着走过的 Deployment 轨迹），
失败给一个 `GatewayError`，Kernel 这才知道该开 Attempt #2。

**可断言形式**（与 H-4"Harness 不持有 Kernel"同一个套路）：

```python
# 1. Gateway 字段里没有任何 Kernel 概念
names = {f.name for f in fields(ModelGateway)}
assert not ({"kernel", "execution", "attempt_no", "lease", "worker"} & names)

# 2. model_gateway 包里连一行 import execution_kernel 都没有
for path in Path(model_gateway.__file__).parent.glob("*.py"):
    for line in path.read_text().splitlines():
        if line.strip().startswith(("import ", "from ")):
            assert "execution_kernel" not in line
```

**对照测试**（把两个维度分开，这是最容易搞混的地方）：

| 场景 | Kernel Attempt 数 | Provider 调用数 |
|---|---|---|
| 3 个 Deployment 全失败（max_fallbacks=3） | **1** | 3 |
| 2 个 Deployment × Kernel 重试一次 | **2** | 4 |

> Attempt 数由 Kernel 决定，调用数由 Gateway 决定。混淆它们就是重试放大。

### Fallback ≠ 降级（G-4）

```text
Fallback   换 Deployment：同一个 Model，不同 provider / region   语义等价   默认允许
降级        换 Model     ：gpt-4o → gpt-4o-mini                  语义不等价  默认禁止
```

必须分开，因为**降级会悄悄改变输出质量**。一个"为了可用性自动降级到小模型"的系统，
出问题时你根本不知道是模型变笨了还是 Agent 写错了。
所以降级要显式开（`allow_degraded=True`），且候选上打 `degraded=True`，审计能查。

### 不变量 G-1 ~ G-7

| # | 内容 |
|---|---|
| G-1 | Gateway 内部 Fallback 不产生新的 Kernel Attempt |
| G-2 | Fallback 次数显式封顶（`max_fallbacks`），总放大由 Run 级 `retry_budget` 兜底 |
| G-3 | `Model ≠ Deployment`：前者是逻辑模型，后者是物理位置 |
| G-4 | Fallback ≠ 降级；换 Model 默认禁止且必须标记 |
| G-5 | 每次调用留下完整 Deployment 轨迹（审计 / 成本归因） |
| G-6 | `ProviderError.retryable` 决定要不要走完整条链 |
| G-7 | Gateway 只上报 token / cost，不判断预算（预算是 Harness 的拦截条件） |

### 接线：只留一条代码路径

`LLMCallExecutor` 现在统一走 Gateway。裸 `LLMClient` 由 `legacy_gateway()` 包成
**单候选 Gateway**，而不是让 Executor 分出一个"直接调用"的分支 ——

> 两条路就等于有两条 Fallback 语义。测试跑通的那条和生产跑的那条不是同一条，
> 那么 G-1 在直接调用那条路上根本没被验证过。

Executor 是 **Kernel 语义的翻译层**：Gateway 只说"模型调用失败"，
`LLMCallExecutor` 才把它翻成 `ExecutorError` + `FailureClass`。

### 本轮修掉的一个真 bug

`ok_response()` 最初写的是 `model_id=request.model_id`（想调的模型），
被 `test_g4_degraded_candidate_is_marked` 抓出来：降级到 gpt-4o-mini 时，
响应里记的却是 gpt-4o —— **成本归因会直接错**，小模型的账算到大模型头上。
改成 `model_id=deployment.model_id`（实际服务的模型）。

> 请求的是谁 vs 实际服务的是谁，在降级场景下是两个不同的值，
> 这种"看起来可以复用同一个字段"的地方最容易埋雷。

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 276 tests ... OK   （新增 28 个：Model Gateway）
```

---

---

## M15 阶段 10：Tool Runtime（§43 实施顺序）— 已完成

### 新增文件

```
packages/agent_runtime/tool_runtime/
├── __init__.py     T-1 ~ T-6 清单
├── spec.py         ToolSpec（含 side_effect 声明）/ ToolResult / ToolProtocol
├── validation.py   极简 Input Validation（不引第三方依赖）
├── protocols.py    ToolInvoker / FunctionInvoker
├── registry.py     Resolution + Version Resolution
└── runtime.py      ToolRuntime（走完 §24 那条流程）

tests/unit/test_tool_runtime.py    31 个用例
```

### §24 流程里的两处职责重叠，实现时必须先裁决

§24 的流程是：

```text
Tool Call → Resolution → Version Resolution → Input Validation
          → Policy → Guardrail → Rate Limit → Timeout → Retry → Executor → Result
```

但 **Policy / Guardrail** 已经被 §2 钉成 Harness 的唯一拦截点，
**Retry** 已经被 Kernel 的 Attempt 统一管了。直接照抄流程就会出现两个判断处。

裁决结果：

| §24 里的环节 | 归属 | 不变量 |
|---|---|---|
| Policy / Guardrail / Rate Limit | **Harness**（不在这里） | T-4 |
| Retry | **Kernel 的 Attempt**（不在这里） | T-1 |

Tool Runtime 保留的是**执行机制**：解析、版本、校验、超时、协议适配。

> T-1 和 Model Gateway 的 G-1 是**同一个问题的两种形态**：
> 单次 Attempt 内部的容错，都不该产生新的 Attempt / 新的外部调用计数。
> Tool Runtime 再重试一层就是 `3 × 3 = 9 次工具调用`。

### T-2：漏传幂等键是硬拒绝

§16 钉死了"Tool Runtime 调用外部系统时透传 idempotency_key（= execution_id）"。
但**怎么保证不漏传**？靠自觉一定漏。

所以 `ToolSpec.side_effect` 是必填项，且：

```python
if spec.needs_idempotency_key and not idempotency_key:
    raise ToolExecutionError("IDEMPOTENCY_KEY_REQUIRED", ..., retryable=False)
```

漏传的后果是重复副作用（重复下单 / 重复扣款），这是最贵的一类 bug，
所以是硬拒绝而不是警告，而且**不可重试**（重试也还是没有 key）。

全链路验证：`Kernel → ExecutionContext.idempotency_key → ToolRuntime → Invoker`，
测试里断言 Invoker 收到的 key 等于 execution_id。

### T-5：默认版本不许随注册顺序漂移

工具升级是**业务事件**，不是部署细节。如果默认版本 = "最后注册的"，
那么注册顺序一变 Agent 行为就变 —— 这种 bug 极难复现。

所以：第一次注册自动成为默认，之后换默认必须 `make_default=True` 明说。

### 又踩了一次"请求值 vs 实际值"的坑

`ToolResult.version` 记的是**实际执行的版本**，不是请求时说的版本。
请求 `latest` 时这两个值不同 —— 记成请求值，审计就无从追溯。
这和 Model Gateway 的 `model_id` 是**同一个坑**（G-4 那轮刚修过一次），
说明这类"看起来可以复用同一个字段"的地方是系统性雷区。

### 不变量 T-1 ~ T-6

| # | 内容 |
|---|---|
| T-1 | Tool Runtime 不做业务重试 —— 归 Kernel 的 Attempt |
| T-2 | `side_effect ≠ READ` 的工具必须带 `idempotency_key`，漏传即硬拒绝 |
| T-3 | Input Validation 失败是 PERMANENT |
| T-4 | Policy / Guardrail / Rate Limit 不在这里 —— Harness 才是唯一拦截点 |
| T-5 | 版本显式解析；默认版本必须显式登记 |
| T-6 | 不关心底层协议（换协议只换 Invoker） |

### 接线：同样只留一条路径

`ToolCallExecutor` 现在统一走 `ToolRuntime`，旧的进程内 `ToolRegistry` 由
`legacy_tool_runtime()` 迁移过来（工具按 `side_effect=READ` 登记 ——
这是兼容路径的**保守假设**，真实工具必须显式声明才能拿到 T-2 的保护）。

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 307 tests ... OK   （新增 31 个：Tool Runtime）
```

---

## M15 阶段 11：完整 Agent Loop（§43 实施顺序）— 已完成

> 命名沿用上两节：本节是 **§43 的 M15 实施顺序第 11 项**。

阶段 9 / 10 造好了两个运行时，但它们此刻还只是"能用"，没有接进 Loop ——
`AgentLoop` 仍然只认 `Worker`，测试里也还是 `LLMCallExecutor(FakeLLM())`。
阶段 11 要做的就是把 §44 那条链**从头到尾接起来，并且全程留痕**。

### §44 的要求不是"跑通"，是"跑通 + 六要素齐全"

```text
POST /agents/{agent_id}/runs
        ↓
     AgentRun ──► Step ──► Task ──► Execution ──► Worker
        ↓                                            ↓
     Finish ◄── Decision ◄── State ◄── Observation ◄──┘
        ↓
AgentRun = COMPLETED
```

并且全过程都要有：

```text
Event / Trace / Attempt / Checkpoint / Cost / Token Usage
```

**少一项不算闭环** —— 即使 `AgentRun.status == COMPLETED` 也是偶然。
所以 `tests/unit/test_agent_loop_full.py` 的结构就是这六项，一项一个用例。

### 本轮新钉的不变量 L-1 ~ L-7

| # | 内容 |
|---|---|
| L-1 | Loop **持有** ModelGateway / ToolRuntime，但不**实现**它们（拿不到 provider / invoker / endpoint） |
| L-2 | Worker 的 Executor 与 Loop 持有的是**同一批对象** —— 一条代码路径 |
| L-3 | Token 用量必须回流到 `CostManager`，否则 `Budget.max_tokens` 是一条接了没通电的线 |
| L-4 | §44 六要素齐全才叫闭环 |
| L-5 | **Step 完成**也要写 Run Checkpoint（§14），不只是"挂起前" |
| L-6 | Trace 是 append-only 的派生审计记录：只有 `append`，没有 update / pop / clear |
| L-7 | 连续被 Harness 拒绝到上限 → Run 判定 FAILED（否则永远空转） |

### 三个真问题（都是装配时才暴露的）

**1）L-3 不是"记账"，是"预算能不能拦住"**

`CostManager` 是 `before_action` 的一环（超预算 → DENY）。在这之前 Loop 只
`harness.charge(steps=1)`，`spent_tokens` 永远是 0 —— 于是 `max_tokens` 这条线
虽然接了，但没通电：超 token 的 Run 会一直跑到钱烧完才停。

修法：`_charge()` 从 Attempt result 的 `usage.total_tokens` 取数回灌。
测试不是断言"有这个字段"，而是把 `max_tokens` 压到 1 —— **不回流就拦不住**。

**2）L-7：被拒不计入任何预算 → 死循环**

这是本轮最危险的一个。`steps` 只数真正执行过的动作，被 DENY 的动作不产生 Task、
不花钱、不占步数。于是"Agent 反复提出一个被禁的动作"这个循环
**既不被预算拦，也不被任何终态拦** —— 它只是每轮多写一条 DENY 的 Observation，
而且**没有任何报错**。

装配 E2E 时直接撞上了：把 `max_tokens` 压到 1，进程跑飞（超时被 kill）。

修法是"连续"而不是"累计"：一次被拒之后换个动作是**正常的收敛行为**，不该受罚；
一直不换才是死循环。所以中间有任何一次非 DENIED 的结果就清零，
连续 `max_consecutive_denials`（默认 3）次 → `_declare_terminal(FAILED)` + `DENY_LOOP`。

**3）G-8：缺省模型该由谁决定**

`LLMCallExecutor` 之前是 `task.payload.get("model") or self.default_model_id`，
默认值 `"default"`。装配 E2E 里 Gateway 注册的是 `gpt-test`，于是第一步就
`NO_CANDIDATE: no usable deployment for model 'default'` ——
**报错指向一个根本不存在的模型名**，排障会被带偏。

根因是边界错了：Executor 是翻译层，它不拥有"有哪些模型"这份知识。
改成 `CompletionRequest.model_id` 允许为空，由 `ModelGateway.default_model_id`
解析；两边都空则报 `NO_MODEL_REQUESTED`（指向配置缺失，而不是假模型名）。

> 这是"请求值 vs 实际值"那个坑的**第三次变体**：
> 前两次是记错（`model_id` / `version`），这一次是**根本没有值却填了一个假的**。

### 装配：为什么要有 `assembly.py`

拼装的每一步都有一个"看起来也行"的错误版本：

| 错误版本 | 后果 |
|---|---|
| Worker 用 `legacy_gateway(FakeLLM)`，Loop 上再挂一个真 Gateway | 两条代码路径，G-1 / T-2 在没走到的那条路上没被验证过 |
| Harness 默认 Budget，Loop 的 `max_steps` 另配一份 | 两个"步数上限"，跑起来不知道是谁先拦的 |
| Clock 各自 new 一个 | 审批超时和 Lease 超时对不上 |

所以 `assemble_runtime_stack()` 里**没有第二个可以造 Executor 的地方** ——
L-2 不是靠自觉，是靠没有别的选择。

顺带踩了一个真值判断的坑：`RunTrace` 有 `__len__`，空账本是 falsy，
`trace or RunTrace(clock)` 会把调用方传进来的那个悄悄换掉。
（症状是 Trace 一直为空，而断言查的是调用方手上那个引用。）

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 323 tests ... OK   （新增 16 个：完整 Agent Loop / §44 闭环）
```

---

## M17：Context / Memory（基线 §21 / §22 / §14）— 已完成

阶段 11 把 §44 那条链接通了，但链上有一个环节还是空的：
**模型到底看到了什么**。在此之前 `LLM_CALL` 的 payload 里只有一个 `prompt`，
"这次调用喂了什么进去"在运行时没有任何记录 ——
排障时唯一能查的是 prompt 字符串本身，而不是它被组装出来的过程。

M17 补的就是这一层。

### §21 的四个对象不是同一个东西的四种叫法

```text
Memory（记住的）  ──► MemoryManager.recall() ──┐
Knowledge（外部资料）──► RetrievalPipeline ────┤
                                              ▼
                                       ContextAssembler（排序 + 取舍 + 预算）
                                              ▼
                                       Context → Model
                                              ▼
                                       ContextSnapshot（审计）
```

§41 早就写了 `Memory ≠ Knowledge ≠ Context ≠ Artifact`，但那是**一句话**，
不落到类型上就等于没有。所以本轮把它们做成**必须显式转换**的四种对象：

- `MemoryRecord` → `ContextItem`：只能经 `MemoryManager.recall()`
- `Chunk`（Knowledge）→ `ContextItem`：只能经 `RetrievalPipeline.to_context_items()`

不转换就进不了 Context。转换口是唯一的，于是 Memory 的内部字段
（`layer` / `subject` / `source_run_id`）不会一路泄漏进 Prompt。

### 不变量 C-1 ~ C-12

| # | 内容 |
|---|---|
| C-1 | Memory / Knowledge / Context / Artifact 是四种对象，转换必须显式 |
| C-2 | Context 是**每次模型调用**的工作台，不是 Run 级状态（一次调用一份 Snapshot） |
| C-3 | Token Budget 是硬约束：装不下就丢；**pinned 装不下直接失败**，不静默降级 |
| C-4 | **静默截断是 bug**：被丢的每条都留 `DroppedItem` + 原因；分配必须确定 |
| C-5 | `ContextSnapshot ≠ Checkpoint`：字段互不重叠（可断言） |
| C-6 | Artifact 只以 `reference` 进 Context |
| C-7 | Memory 分层；**Qdrant ≠ Truth**（索引派生、可重建） |
| C-8 | Memory 写入可溯源；EPISODIC 必须带 `source_run_id` |
| C-9 | Retrieval **必须**过 Permission Filter；权限不能外包给 Retriever |
| C-10 | Knowledge 进 Context 必须带 Citation |
| C-11 | **组装归 Runtime，准入归 Harness** |
| C-12 | **排序（语义）与取舍（priority）是两个维度** |

### 四条值得单独说的

**C-3：pinned 装不下时应该失败，不是降级**

系统指令或工具契约放不进上下文窗口，继续跑下去只会得到一个
**行为完全不同的 Agent**，而且不会报错。所以 `ContextBudgetError` 是硬失败 ——
这类情况是配置错误（模型窗口 / 预算配错了），早失败早修。

**C-4：静默截断是 bug**

"模型没看到工具列表"和"模型看到了但没用"是两种完全不同的故障。
只截断不记录的话，事后无法区分。所以每条被丢的内容都留 `DroppedItem` + 原因，
并且分配结果**必须确定**（同样的输入永远同样的输出）——
不确定的话 Snapshot 就不是审计记录，而是又一次随机采样。

**C-9：不过滤的检索就是越权**

向量库里存了全公司的文档，检索时如果不带权限过滤，
Agent 就成了"绕过权限的搜索框"，而且**不会报错** ——
它只是老老实实把不该看的内容喂给了模型。

所以 `RetrievalPipeline.permission` 是构造期必填，没有"先不过滤"这个选项。
而且过滤器要在 pipeline 里**再跑一遍**：
只靠 Retriever 的 push-down 过滤，等于把安全边界外包给了存储层 ——
换一个 Retriever 就没了。

**C-11：§23 的一处归属需要裁决**

§23 写着 `AgentHarness ├── ContextManager ├── MemoryManager`，
按字面读像是"Harness 负责组装 Context"。但 §2 的四边界说 Harness **控制**、
Runtime **驱动**，而把内容拼进 Prompt 明显是执行的一部分。

裁决：**组装归 Runtime，准入归 Harness**。
§23 的 `ContextManager` 指的是后者（权限 / 敏感信息 / 越权检索的准入），
不是让 Harness 去拼字符串。这条已回写基线。

### 接线

- `CompletionRequest` 新增 `context: tuple[str, ...]` —— Context 随 Task 走到 Executor，
  再由 Executor 交给 Gateway。Gateway 不认识 Context 是怎么来的，但它会出现在轨迹里。
- `AgentLoop.context_assembler` 可选；没配就按老样子只发 prompt（不留半吊子分支）。
- `ContextAssembler` 上留了 `memory_provider` / `knowledge_provider` 两个槽，
  对应 §22 的 `MemoryAssembler` / `RetrievalAssembler`。

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 357 tests ... OK   （新增 34 个：Context / Memory）
```

---

## M18：Control Plane 契约层 + HITL 审批回调（基线 §44 / §23 / §37）— 已完成

### 动因：§44 的第一行没有落点，而且人根本没法批准

链通了、模型也看得见东西了，但闭环图的**最上面一格**在代码里不存在：

```text
POST /agents/{agent_id}/runs      ← §44 的第一行，此前没有任何落点
        ↓
AgentRun → Step → Task → Execution → ...
```

更尴尬的是 HITL：在此之前 `AgentLoop.approve()` **只是一个进程内方法**。
高风险动作被闸门挡住之后，Run 永远停在 `waiting_approval` ——
Kernel 里那条 `SUSPENDED(HUMAN_APPROVAL)` 的 Execution 在等一个不会来的信号，
而系统里**没有任何入口**能让真人把"批准"递进去。

HITL 在架构上是完整的（Harness 发起 / Kernel 拥有生命周期 / Wake-up 检测 / Scheduler 重调度），
但在工程上是死的。

### 落点

```text
packages/agent_api/
  errors.py     领域异常 → HTTP 语义（A-2）+ A-9 的语义切分
  dto.py        StartRunRequest / RunView / ApprovalView / DecisionRequest
  ports.py      ControlPlane 协议（先有 Port，HTTP 层才不会自己判断业务）
  service.py    InProcessControlPlane（真实实现跨进程 + DB，契约不变）
  handlers.py   与框架无关的 handler（A-7）
```

### 三条最值得钉住的

**A-4：审批回调必须经 `AgentLoop.approve()`。**
不能让 HTTP 层直接改 `ApprovalRequest.status` —— 那样 Kernel 里那条 SUSPENDED
的 Execution 不会被唤醒，Run 就永远挂在等一个"已经批过了"的审批上。

**A-9：过期 ≠ 已决定。**
`ApprovalStatus` 里 EXPIRED / CANCELLED 和 APPROVED / REJECTED 都是"非 PENDING"，
但 HTTP 语义完全不同：

| 状态 | HTTP | 为什么 |
|---|---|---|
| APPROVED / REJECTED | 409 `APPROVAL_ALREADY_DECIDED` | 有人决定过；带 `decided_by` 让调用方自己去看 |
| **EXPIRED** | **410** `APPROVAL_EXPIRED` | 没人决定过它，是时间判死的 |
| CANCELLED | 409 `APPROVAL_CANCELLED` | 系统主动撤销 |

最容易写错的一版是把超时也报成 409 `APPROVAL_ALREADY_DECIDED`（`decided_by="timeout"`）：
语法完全正确，语义上却是在告诉调用方"有人批过了，去等结果吧"——而那个结果永远不会来。
**审计要回答的恰恰是"到底有没有人批过"。**

配套的一条裁决：存储里还写着 PENDING 但时间已过（sweeper 还没扫到）时，API **只读地报 410，
不把状态推进成 EXPIRED**。推进是 `HumanLoop.expire_due()` 的职责，与 Cancellation 的 sweep 同构 ——
否则"被人判死的"和"被时间判死的"混在一起，审计就废了。

**A-10：待审批列表查存储，不查内存 Loop。**
读 `loop.pending_approval` 的后果：服务一重启，列表就空了，而 Run 还实实在在挂着等人批 ——
**界面上什么都没有，系统里全在等**。这是最难排查的一类故障。
测试里直接往存储写一条 Loop 没在等的审批，断言它必须出现在列表里（此时 Loop 只在等一条）。

### 不变量 A-1 ~ A-10

| # | 内容 |
|---|---|
| A-1 | API 不做业务判断：只校验形状 / 编排 / 翻译；状态变更一律走 Runtime / Kernel |
| A-2 | 领域异常必须映射成明确 HTTP 语义；**500 只留给真的没预料到的** |
| A-3 | `POST /runs` 幂等；**幂等键不能放 Redis** |
| A-4 | 审批回调是唯一入口，且必须经 `AgentLoop.approve()` |
| A-5 | 归属校验 404（不泄漏别的 Run）；已决定再回调 → 409，不能静默成功 |
| A-6 | 响应不暴露 Kernel 内部对象 |
| A-7 | handler 与框架无关 |
| A-8 | 审批必须带 `by` |
| A-9 | **过期 ≠ 已决定** |
| A-10 | 待审批列表查存储不查内存 |

其中 **A-1 是可断言的**：`packages/agent_api` 连 `policy` / `guardrail` / `cost` 都 import 不到 ——
"API 自己判断这个动作该不该做"在**结构上**不可能，不用靠自觉也不用靠 code review 抓。

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 384 tests ... OK   （新增 27 个：Control Plane 契约层 + HITL 审批回调）
```

---

## M19：审批存储持久化（基线 §23 / §20）— 已完成

### 动因：A-10 是一条断言，不是一个事实

M18 写下 A-10 时，理由写得很硬："审批必须活过进程重启"。
但当时它在代码里**并不成立** —— `ApprovalStore` 虽然是 Port，唯一实现却是 `InMemoryApprovalStore`。

这类缺口最危险的地方是：写的人和读的人都会默认它已经成立了。
所以本轮的目标很窄：**把 A-10 从断言变成事实**。

### 落点

```text
infrastructure/postgres/003_approvals.sql      表 + 四条审计约束（A-13）
packages/agent_harness/adapters/postgres.py     PostgresApprovalStore
packages/agent_harness/approval.py             ApprovalStore.transition()（A-11）
packages/agent_runtime/assembly.py             approval_store= 共享下传
packages/agent_api/service.py                  ControlPlane 持有存储；列表只查存储
```

### 三条最值得钉住的

**A-11：判定与写入必须是一次原子操作。**
A-5 说"已决定的再回调 → 409"，但它防的是**先来后到**，防不住**同时到达**：

```text
请求 A  读 → PENDING        写 → APPROVED(by=alice)
请求 B    读 → PENDING      写 → APPROVED(by=bob)     ← 覆盖了 alice
```

两边的前置检查都通过了，因为**它们读到的都是 PENDING**。
所以判定与写入必须是一个操作，由存储保证：

```sql
UPDATE approvals SET status=%s, decided_by=%s, ...
 WHERE approval_id=%s AND status='pending'     -- rowcount=0 就是输了这场竞争
```

测试里留了一条反向用例（`test_a11_the_hole_this_closes_is_real`）专门演示这个洞：
两次 `save()` 都是合法写入，结果 `decided_by` 变成了后到的 `bob`。

**A-12：存储选型的判据是"丢了之后变慢还是变错"，不是"重不重要"。**

| 数据 | 丢了会怎样 | 放哪 |
|---|---|---|
| Lease 索引 | 变慢（可从 PG 重建） | Redis（快路径） |
| 审批记录 | **变错**（Run 永远挂着，界面上看不见） | PG（事实源） |

**DB 约束能拦什么，不能拦什么。**
补 A-11 时先写了一条"演示这个洞"的测试，它失败了，但失败方式出乎意料 ——
`sqlite3.IntegrityError: CHECK constraint failed: approvals_decided_has_time`。

原因是我演示覆盖写入时没填 `decided_at`。于是得出一条边界：

| 层次 | 管什么 |
|---|---|
| DB CHECK 约束 | **值的形状**（已决定必须有 by / decided_at） |
| A-11 原子 transition | **先后顺序**（谁先写到谁算数） |

两次写入都合法时约束完全放行，后写的照样覆盖先写的。
**约束管形状，不管时序** —— 所以 A-13 不能替代 A-11，两者都得有。

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 401 tests ... OK   （新增 17 个：审批存储持久化）
```

其中 `RestartSurvivalTest` 有两条互相对照的用例：
换成 PG 存储，"重启"后列表里还有东西；换成内存存储，同一个"重启"就什么都没有了 ——
后一条存在的意义是证明前一条**不是白测的**。

### 已知缺口（已由 M20 闭合）

`decide()` 仍然要求 Run 在内存里（`_must_stack`）。服务重启后，
Run 需要**从 PG 重新装载 RuntimeStack** 才能被批准 —— 那属于 Recovery 范畴，
不在本轮范围，记为下一轮。

> **M20 已闭合**：`RunSnapshot` + `004_run_snapshots.sql` + `RunRecovery`
> 让"重启 → 批准"从 404 变成可用，见下节。

---

## M20：Run Recovery（基线 §14 / §22 / §37）— 已完成

### 动因：M19 只让审批"看得见"，没让它"点得动"

M19 之后，服务重启时待批列表还在（A-10 兑现了），但点"批准"会得到 404 ——
因为 `decide()` 要求那个 Run 的 `RuntimeStack` 还在内存里。

这比列表为空更糟：**列表为空是"看不见"，这个是"看得见却办不了"。**

根子上是 `RunCheckpoint` 被误当成恢复数据，而它其实是**指针**：

| | 是什么 | 能不能用于恢复 |
|---|---|---|
| `KernelCheckpoint` | 单条 Execution 的续跑位置 | 只够续跑一条 Execution |
| `RunCheckpoint` | 指向"某个 Checkpoint"的引用（E-24 禁止它带 current_step / completed_tasks） | **不能** |
| `RunSnapshot`（新增） | Run 级完整快照：State + Steps + 预算计数 + Trace + 待批 id | **能** |

### 落点

```text
packages/agent_domain/business/snapshot.py       RunSnapshot + 通用序列化
packages/agent_runtime/recovery.py               RunSnapshotStore Port + RunRecovery
packages/agent_runtime/adapters/postgres.py      PostgresRunSnapshotStore
infrastructure/postgres/004_run_snapshots.sql    JSONB 存 state/steps/spent/trace，被查的字段提成列
packages/agent_runtime/loop.py                   capture() / restore()
packages/agent_runtime/trace.py                  RunTrace.restore() + RECOVERED / SNAPSHOT
packages/agent_runtime/assembly.py               snapshots= 共享下传
packages/agent_api/service.py                    _recover() 在 _must_stack 之前
packages/agent_domain/execution/execution.py     store_version（E-25）
```

### 五条不变量

| # | 内容 |
|---|---|
| R-1 | 进入 SUSPENDED 必须同时落 Snapshot —— `RunCheckpoint` 是指针不是数据 |
| R-2 | 恢复必须带上预算计数与已花费 —— 否则"挂起—恢复"是重置预算的后门 |
| R-3 | 终态 Run 不可恢复；`IllegalTransition` 是 **409 不是 404**（"有，但不许" ≠ "没有"） |
| R-4 | 审计账本必须延续：run_id 一致 + 序号连续 + 留一条 `run.recovered`；断号拒绝拼接 |
| R-5 | 恢复归 Runtime，不归 API —— 契约层不许自己拼装 `RuntimeStack` |

R-1 里有个**顺序**问题值得单记：`RunCheckpoint` 必须写在 `kernel.suspend()` **之前**（§14 强制），
`RunSnapshot` 必须写在**之后** —— 因为它要带上 `pending_approval_id`。
两者不能合并成"挂起前写一次"。

### 撞出来的两个真缺陷，都不是 Recovery 的

**X-13：跨 Port 拿到的是副本，不是同一个对象。**

```python
self.harness.approvals.bind(approval.approval_id, execution.execution_id)   # ❌ 丢了返回值
approval = self.harness.approvals.bind(...)                                  # ✅
```

`bind()` 改的是**存储里那条**，手上这个 `approval` 不会跟着变，于是 `execution_id` 一直是 `None`，
`_close_gate()` 提前返回，那条 SUSPENDED 的 Execution **永远醒不过来，而且没有任何报错**。

> 内存版"碰巧正确"，因为 `InMemoryApprovalStore.get()` 返回同一个对象。
> 跨 Port 的语义只能是副本 —— 端口另一边可能是另一个进程、另一台机器。
> **用内存实现去验证"跨边界"的行为，验证的是巧合。**

**E-25：乐观锁到底在比什么。**

`resume()` = `SUSPENDED → PENDING → RUNNING`，**两次**状态跃迁（中间还新建了一个 Attempt），
但 Kernel 只 `_persist()` 一次。乐观锁却拿 `previous_version`（上次**内存自增前**）= 4 去比对，
库里还是 3：

```sql
UPDATE executions SET ... WHERE execution_id = %s AND version = 4   -- 命中 0 行
```

**没有任何并发，乐观锁自己报错。** "一次内核操作 = 一次自增 = 一次落库"这个隐含假设从不成立。

修法：给 `Execution` 加一个**只能由 Repository 改写**的 `_store_version`。

| 字段 | 含义 | 谁能改 |
|---|---|---|
| `previous_version` | 上一次**内存自增前**的版本 | 域模型 |
| `store_version` | 上一次**存储边界**的版本 | Repository（`add` / `get` / `save`） |

乐观锁比的是"**我读到的**版本"，不是"我上次自增前的版本"。
副作用是 `resume()` 后版本号从 3 直接跳到 5 —— 这是对的，两次跃迁合成一次写。
（`recover()`（E-23）只自增一次，不受影响。）

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 420 tests ... OK   （新增 19 个：16 个恢复 + 3 个 E-25）
```

`RestartThenDecideTest` 有互相对照的两条：给了快照存储，"重启"后批准成功；
不给快照存储，同一个"重启"就 404 —— 后一条证明前一条**不是白测的**。
E-25 的三条里也留了一条对照（`test_e25_the_control_...`），
把"修之前的行为"钉在仓库里，免得哪天被改回去而没人发现。

---

## M10：Saga / Compensation（基线 §42 / §23 / §24）— 已完成

### 动因：系统会制造状态，却不知道自己制造过什么

M15~M20 把"可靠地把一个动作执行完"做得相当充分：Attempt / Lease / Retry / Recovery / 快照恢复都有了。
但缺一环 —— **Agent 是会在外部世界留下东西的**（下单、开工单、发消息、调支付）。

```text
Run 失败 → 已经发生的副作用就那么留着 → 没有任何报错
```

系统不记"我在外部留下了什么"，于是根本不会有人去查。

### 落点

```text
packages/agent_domain/business/compensation.py     CompensationSpec / Record / Status
packages/agent_domain/intelligence/action.py       Action.compensation（S-8 / S-13）
packages/agent_runtime/saga.py                     CompensationStore Port + SagaCoordinator
packages/agent_runtime/adapters/postgres.py        PostgresCompensationStore（S-2 / S-4）
infrastructure/postgres/005_compensations.sql      UNIQUE(execution_id) + 值域约束
packages/agent_runtime/loop.py                     登记 / 撤销 / 独立 Step / 不计预算
packages/agent_harness/harness.py                  before_action 连同逆操作一起审（S-9）
packages/agent_runtime/trace.py                    compensation.* 六种 kind
```

### 最重要的一条：M10 不新增执行通道（S-1）

撤销动作就是一条普通的 `Task → Execution`，Retry / Lease / Attempt / Idempotency 全部复用 Kernel。
否则会出现两套可靠性语义。

| 问题 | 归谁 |
|---|---|
| 要不要撤销、按什么顺序、撤销不掉怎么收尾 | **Runtime**（`SagaCoordinator`） |
| 把撤销动作可靠地跑完 | **Kernel**（它不知道这是撤销） |
| 逆操作能不能用 | **Harness**（正向提出时一起审，S-9） |

### 十六条里最值得钉住的三条

**S-9：批准一个动作 = 同时批准它的撤销。**
撤销阶段**故意绕过** `before_action`：撤销时再等人批准，"人走了、Run 失败了"
就会让副作用永久留着，而且不报错（系统确实在"等批准"）。
既然撤销时不再审一次，"一起审过"就必须发生在正向动作提出时 ——
于是 `before_action` 里对 `action.compensation` 做一次真实 Policy 评估，
逆操作被禁 → 整个动作都不许做：

```text
一个不能撤销的高风险动作，不该被放出去。
```

> 这条一开始只是注释里的一句断言 —— 和 A-10 同一个坑。
> 本轮把它从断言改成了事实（测试断言 `Verdict.DENY` 且 reason 含 S-9）。

**S-16：成功的 Run 必须结案。**
写测试时才发现的洞：Run 成功 → 副作用按预期保留 → 但账本里那条还是 `PENDING`，
而 PENDING 的字面意思就是"待撤销"。于是每一次成功运行都留下一串假待办，
真正要撤销的那几条会淹没在里面。

不能记成 `COMPENSATED`（那是"撤销过了"，另一句谎话），所以新增 `NOT_NEEDED`。

**S-6：单条失败不阻断其余。**
一条撤销动作坏掉就停下来，等于用一个局部故障换一批永久性副作用。
所以继续跑完，最后统一记 `UNRESOLVED`（必须带 reason，DB 有约束兜底）。

### 验收

```bash
python -m unittest discover -s tests -q
# Ran 450 tests ... OK   （新增 30 个：Saga / Compensation）
```

带对照的几条：

| 用例 | 对照 |
|---|---|
| 声明了补偿 → 失败后被撤销 | 没声明 → 什么都不撤销（证明不是白测） |
| 逆操作被策略禁 → 正向 DENY | 没有那条禁令 → 同一个动作放行 |
| 成功 → 账本结案 | 结案之前那条确实是 PENDING（证明不是"压根没登记"） |
| PG 认领：后到者拿不到 | 两个独立读视图都看到 PENDING，但只有一次认领成功 |

### 已知缺口（本轮未做）

- 补偿**重试**：单条撤销失败只记 `UNRESOLVED`，没有自动重扫（需要 `compensation_retry_controller`）
- 补偿的**超时**：撤销动作目前用 Task 默认 timeout，没有独立的（更短的）撤销 SLA
- `AGENT_DELEGATION` 的递归补偿：子 Run 的副作用目前不在父 Run 的账本里

---

## 7 · 最小闭环验收

```
POST /agents/{agent_id}/runs
        ↓
    AgentRun(CREATED)  ──► Goal Interpreter ──► Goal
        ↓
    Agent Loop → Decision Engine → Decision → Action(TOOL_CALL)
        ↓
    TaskFactory ──► Task(idempotency_key=...)
        ↓
    Execution Kernel: Execution(QUEUED → RUNNING) + Attempt#1 + Lease
        ↓
    Worker → LLM → Tool
        ↓
    Observation ──► StateReducer ──► State.apply()
        ↓
    Attempt#1 SUCCEEDED → Execution COMPLETED → AgentRun COMPLETED
        ↓
    Final Answer
```
